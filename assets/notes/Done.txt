fix(home-songs): correct album toolbar order key and simplify search button
  - `HomeAlbumsToolbarSettingsDialog.kt`: Changed tab key from `"all"` to `"lib"` and preference key from `homeAlbumsToolbarOrderKey` to `homeAlbumsLibraryToolbarOrderKey`
  - `HomeSongsScreen.kt`: Removed conditional match button from search toolbar — now shows only search icon
  - `LocalPlaylistToolbarSettingsDialog.kt`: Added `removeFromPlaylistLabel` string resource for future use
  - Created `RemoveFromPlaylist.kt` dialog component in legacy package for playlist song removal

refactor(library): clarify playlist deletion dialogs and button labels
  - Differentiated delete playlist dialog strings from songs smart trash (avoid confusing "remove from all playlists permanently")
  - `HomeLibrary.kt`: Replaced `smart_trash` string with `delete_playlists` for the delete button label
  - `HomeLibrary.kt`: Added custom dialog text for multi-select delete vs delete all playlists
  - `LocalPlaylistSongs.kt`: Added clear dialog text for removing songs from playlist when using multi-select
  - Created and translated new string resources for delete playlist confirmations in `strings.xml` and `values-fr/strings.xml`

refactor(export): remove name edit dialog for single cache export, match batch flow
  - Removed `TextInputDialog` inheritance from `ExportCacheDialog` — now extends `Dialog` directly
  - Removed `valueState`, `keyboardOption`, `allowEmpty`, `onSet`, `onValueChanged` (no longer needed)
  - `onShortClick()` calls `showDialog()` → `Render()` shows `ValueSelectorDialog` (lyrics type) → folder picker → auto-generated filename
  - Filename auto-generated as `title - artists` (same as batch), illegal characters replaced
  - Lyrics type selection now uses `ValueSelectorDialog` matching the batch export flow in `HomeSongsScreen`
  - Fixed `isExporting` access in `SongItemMenu` and `PlayerItemMenu` (`MutableState<Boolean>` → `.value`)
  - Fixed pre-existing `LocalPlaylistSongs.kt` missing `dialogTitle` implementation in anonymous `Dialog` object

feat(ui): migrate Material 3 Checkbox to custom Icon system globally
  - Replaced `Checkbox` with custom `checked_filled` and `unchecked_outline` icons in `RenameSongDialog`, `ExportBackupDialog`, `CheckboxDialog`, `ToggleListDialog`, etc.
  - Set `unchecked_outline.xml` intrinsic width/height to 24dp (was 48dp) to match `checked_filled.xml` and prevent huge sizing in places like `SongItem`
  - Ensured rows wrapping the icons use `Modifier.clickable` properly for better UX
  - Fixed `basicMarquee` text truncation in `CheckboxDialog` (used by `ResetSongDialog`) by adding `fillMaxWidth()` to Row and `weight(1f)` to BasicText

feat(library): smart trash and new playlist adjustments
  - Added Trash icon to Library tab for easier playlist deletion
  - Removed "New Playlist" button from Home Library since "Add to Playlist" is already present

feat(home): batch export cached/downloaded songs with full metadata from Home toolbar (issue https://github.com/N-Zik-Group/N-Zik/issues/XXX)
  - Added `batchExport` companion function to `ExportCacheDialog` for batch export with folder picker
  - Added `showResultToast` parameter to `onExport` to suppress per-song toasts in batch mode
  - Removed separate `ExportCachedSongsDialog` class — single dialog now handles both single and batch export
  - `HomeSongsScreen`: batch flow uses confirm dialog → ValueSelectorDialog (lyrics type) → folder picker → `ExportCacheDialog.batchExport` → InProgressDialog
  - Full metadata parity with single-song export: artwork (JPEG/METADATA_BLOCK_PICTURE for Opus), genre, copyright, composers, description parsing from YouTube
  - Proper SAF URI handling: `getTreeDocumentId` + `buildDocumentUriUsingTree` for `DocumentsContract.createDocument`
  - Persistable URI permission taken for folder write access
  - Lyrics export as separate `.lrc` files: Synced (LRC), Karaoke (extended LRC), Unsynced (plain text)
  - `application/octet-stream` MIME type for .lrc to avoid system appending `.txt` extension
  - Fixed `export_cache` button visible in all toolbar settings tabs (now only Offline/Downloaded)

fix(ui): swap download/delete toolbar button icons (issue https://github.com/N-Zik-Group/N-Zik/issues/587)
  - `DownloadAllSongsDialog.iconId` changed from `R.drawable.download` to `R.drawable.downloaded`
  - `DeleteAllDownloadedSongsDialog.iconId` changed from `R.drawable.downloaded` to `R.drawable.download`
  - `HomeSongsToolbarSettingsDialog` swapped icons for `download_all` and `delete_downloads` ToggleItems
  - `LocalPlaylistToolbarSettingsDialog` swapped icons for `download_all` and `delete_downloads` ToggleItems
  - `Dialog.kt` GitHub download button changed from `R.drawable.downloaded` to `R.drawable.download`
  - Icons now match their actions: download_all shows checkmark (downloaded), delete_downloads shows arrow (not downloaded)

feat(playback): add pause on headphone disconnect setting (issue https://github.com/N-Zik-Group/N-Zik/issues/749)
  - New `pauseOnHeadphoneDisconnectKey` preference in Preferences.kt
  - `isPauseOnHeadphoneDisconnectEnabled()` accessor in GlobalVars.kt
  - Custom `audioBecomingNoisyReceiver` in PlayerServiceModern for runtime preference check
  - ExoPlayer `setHandleAudioBecomingNoisy(false)` — receiver handles it instead
  - Toggle added in General Settings (instant, no restart needed)
  - Default ON — pauses on headphone disconnect out of the box
  - Unit tests in PauseOnHeadphoneDisconnectTest.kt (3 tests)

Fixed android-37 SDK issue
- Unified compileSdk to 37 across all modules (ComposeN-Zik, betterlyrics, nextvisualizer, discordrpc)
- Removed corrupted android-35 and android-37.0 SDK platforms (missing android.jar)
- Reinstalled Android 17.0 (API 37.1) SDK platform

feat(download): skip already-downloaded songs in Download All and fix progress counter (issue https://github.com/N-Zik-Group/N-Zik/issues/588)
  - `MediaDownloadDialog.onConfirm()` now filters out songs with `STATE_COMPLETED` before deleting cache/Format
  - `MyDownloadHelper` gains `batchTotal`, `batchCompleted`, `isBatchActive` fields with `@Synchronized` access
  - `MyDownloadService.getForegroundNotification()` uses batch counters instead of `maxActiveDownloads` for accurate "X / Y" progress
  - `DeleteAllDownloadedSongsDialog` overrides `onConfirm()` with reversed filter to avoid breaking delete functionality
  - Skipped songs counted toward progress via `skipBatchCompleted()` to prevent notification stuck below 100%
  - `resetBatch()` called in `TerminalStateNotificationHelper.onIdle()` to clean up state

Stream Resolution Fixes
- ANDROID_VR: HEAD 403 now marks client as failed and skips to next (no more infinite retry)
- Cipher refresh now passes currentPlayerHash to PlayerConfigStore (matches Metrolist)
- HEAD validation skips for last fallback client to guarantee one stream reaches ExoPlayer
- isExpiredUrlError() now handles 401/403/410 (was missing 401)
- HTTP status code check before parsing PlayerResponse (prevents NULL status from bad responses)
- Non-web clients marked as failed on HTTP errors during player request

Network Fixes
- expectSuccess = false (Ktor doesn't throw on non-2xx, we handle errors manually)
- HttpTimeout plugin added: 60s request, 30s connect, 60s socket
- Chrome User-Agent for WEB/WEB_REMIX (was Firefox)

Client Fixes (previous session)
- All clients synced with innertubex (versions, flags, userAgent)
- useMusicPlayerEndpoint flag added to ANDROID_VR, VISIONOS, TVHTML5_SIMPLY
- includeUserAgentInContext added to ANDROID_VR, MOBILE, TVHTML5, IOS, IPADOS, ANDROID_CREATOR
- requirePoToken added to TVHTML5_SIMPLY, TVHTML5_SIMPLY_EMBEDDED_PLAYER
- buildId/cronetVersion/packageName added to ANDROID_VR_1_43_32, ANDROID_VR_1_61_48, ANDROID_CREATOR

Bug Fixes (previous session)
- PlayerServiceModern: wrong thread crash fixed (Dispatchers.IO -> Dispatchers.Main)
- PlayerServiceModern: async cipher refresh (no more runBlocking on rejection)
- StreamResolver: per-client failure tracking with 5min TTL (ConcurrentHashMap)
- PlayerServiceModern: isExpiredUrlError() detects 410 Gone

Error Reporting & Diagnostics
- Crash logs now include full device info: manufacturer, model, device name, Android version (SDK), board, bootloader, fingerprint, hardware, package name
- Startup banner logs app version + device info at boot (Timber tag "Startup")
- CaptureCrash accepts Context for package name in crash reports
- playabilityStatus.reason from YouTube shown in error toasts (detailed message)
- getErrorCodeName() maps 30+ ExoPlayer error codes to human-readable names
- Error toasts show "Playback failed: <reason> [<code>]" format
- LoginRequiredException / age-restricted detection with dedicated error strings

Cookie Expiry Detection
- MainApplication.CookieStatus enum (NOT_LOGGED_IN, VALID, INVALID, EXPIRED)
- Startup cookie validation checks SAPISID/LOGIN_INFO + persisted ytCookieExpiredKey flag
- Toast on app start if cookie is INVALID or EXPIRED
- 401 detection in PlayerServiceModern & StreamResolver sets cookieStatus=EXPIRED
- Settings card warning when cookie is INVALID/EXPIRED
- Disconnect/disable/re-enable/import resets cookie status and clears persisted flag

YouTube Login Refresh
- Account info (name, email, handle, thumbnail) refreshes immediately after login without restart
- YouTubeSync switch greyed out with "(WIP)" label

StreamResolver Detailed Reason
- firstYouTubeReason captures first YouTube playability reason (not HTTP errors)
- Final throw prefers YouTube reason over generic HTTP error for better user messages

fix(multi-select): actions now respect active selection (issue multi-select bugs)
  - HomeSongsScreen.kt: Match uses `getSongs()` instead of `itemsOnDisplayState` (2 locations)
  - LocalPlaylistSongs.kt: Match uses `getSongs()` instead of `items`
  - ArtistDetails.kt: Shuffle/PlayNext/Enqueue use `getSongs()` instead of `fetchAllArtistSongs()` (3 locations)
  - Queue.kt (component): ShuffleQueue accepts optional `itemSelector` parameter for selection-aware shuffle
  - Queue.kt (main): passes `itemSelector` to ShuffleQueue
  - Pattern: `getSongs() = itemSelector.ifEmpty { items }` returns selected items or all items

fix(home-songs): correct Top playlist sort settings — use StatisticsType periods instead of SongSortBy
  - HomeSongsSortSettingsDialog: Top tab now uses `topSortIds` (Today, OneWeek, OneMonth, ThreeMonths, SixMonths, OneYear, All) instead of incorrectly reusing SongSortBy options
  - Top tab's sort is PeriodSelector (StatisticsType), not SongSortBy — the previous filter on songSortIds was semantically wrong
  - getSortIcon / getSortLabel updated with entries for all StatisticsType names

feat(home-songs): Top playlist PeriodSelector respects custom order and visibility
  - PeriodSelector: accepts optional `sortMenuOrderKey` and `sortMenuPrefix` parameters
  - Added `readSortedEntries()` mirroring Sort.readSortedEnumConstants() — reads saved order + visibility from SharedPreferences
  - MenuComponent now uses `readSortedEntries()` instead of hardcoded `StatisticsType.entries`
  - Each entry's own `iconId` is now shown in the menu (was hardcoded `R.drawable.time`)
  - HomeSongs.kt and HomeSongsScreen.kt pass `homeSongsTopSortMenuOrderKey` + `"top"` prefix to PeriodSelector

fix(multiselect): shared selection state across all screens
  - ItemSelector.kt: `selected` backed by `MutableState<ArrayList>` (remembered) — fixes stale selection across recompositions
  - Queue.kt (component): removed `remember` from toolbar buttons list
  - HomeSongsScreen.kt: removed `remember` + `mutableStateListOf` from toolbar buttons
  - LocalPlaylistSongs.kt: removed `remember` + `LaunchedEffect` sync pattern from toolbar buttons
  - All screens now use ItemSelector's shared state correctly

fix(queue): shuffle and delete actions no longer crash or close player
  - ShuffleQueue: uses reverse remove + add instead of setMediaItems (preserves player state)
  - DeleteFromQueue: uses reverse removeMediaItem instead of setMediaItems
  - Both wrapped in try-catch with Timber logging
  - Added toaster feedback for shuffle success and delete success

fix(multiselect): unified dialog titles with item counts
  - DeleteFromQueue: shows "Remove X songs from queue?" when items selected
  - DownloadAllSongsDialog: shows "Download all X songs?" with count
  - DeleteAllDownloadedSongsDialog: shows "Delete all X downloads?" with count
  - ResetCache: shows "Clear cache for X songs?" with count
  - SmartTrash: already had count support (no change needed)
  - New string resources: clean_queue_confirm_selected, *_count variants

feat(home-songs): export cached/downloaded songs as audio files via FFmpeg
  - New ExportCachedSongsDialog: batch exports cached/downloaded songs to .m4a/.ogg with metadata (title, artist, album, date, track, copyright)
  - Uses OpenDocumentTree folder picker for batch destination
  - FFmpeg transmux with metadata tagging (reuses ExportCacheDialog logic)
  - Skips non-cached/non-downloaded songs automatically
  - Fixed duplicate export bug: removed redundant "export_downloaded" (was same as export_dialog CSV)
  - Button available on both Cached (Offline) and Downloaded tabs
  - Toolbar settings updated: export_cache replaces export_downloaded in allButtonIds and tabAvailableIds
  - New string: do_you_really_want_to_export_cached_count

perf(home-songs): optimize Home lists performance — eliminate lag on Downloads/Cached tabs
  - `HomeSongs.kt`: Pre-compute `downloadedIds` as `Set<String>` via `derivedStateOf` — O(1) lookup instead of O(n) per item with `List<String>`
  - `HomeSongs.kt`: Pre-compute `cachedSongIds` in `LaunchedEffect` — `binder.cache.isCached()` called once at load instead of N times per item
  - `HomeSongs.kt`: Pre-compute `downloadStatesMap` (`Map<String, DownloadedStateMedia>`) — eliminates per-item Flow subscriptions for cache state
  - `HomeSongs.kt`: Replace `collectAsState()` with `collectAsStateWithLifecycle()` across all tabs (All, Favorites, Top, Downloaded, Cached)
  - `HomeSongs.kt`: Wrap `itemsOnDisplay.clear() + addAll()` in `Snapshot.withMutableSnapshot` for atomic diffing — fixes LazyColumn recomposition dance
  - `HomeSongs.kt`: Remove `distinctBy { it.id }` from `itemsIndexed` — now computed once in `LaunchedEffect` instead of every recomposition
  - `OnDeviceSongs.kt`: Same `Snapshot.withMutableSnapshot` + `distinctBy` removal as HomeSongs
  - `SongItem.kt`: Add optional `precomputedCacheState` and `precomputedDownloadState` params — bypasses 3-4 Flow subscriptions per item when provided
  - `DownloadUtils.kt`: Replace 5 `collectAsState()` calls with `collectAsStateWithLifecycle()` — Flows suspend when app is in background
  - `MediaItemUtils.kt`: Replace 4 `collectAsState()` calls with `collectAsStateWithLifecycle()`
  - Impact: ~300-400 Flow subscriptions eliminated for 100+ visible items, scroll FPS target ≥55

Improved:
- improve(ui): stabilize Home tab headers and cache indicator animations
  - `HomeSongsScreen.kt`: Hoisted `Header()` out of `LazyColumn` into the parent `Column` to prevent full subtree destruction and layout jumps when switching tabs
  - `HomeSongsScreen.kt`: Wrapped `CacheSpaceIndicator` in `AnimatedVisibility` to prevent it from instantly popping when switching to Downloaded/Offline tabs
  - `HomeSongs.kt` & `OnDeviceSongs.kt`: Added `animateItem()` to `SongItemPlaceholder` and `isLoading` states for smooth shimmer-to-content transitions
  - `CacheSpaceIndicator.kt`: Wrapped progress bar in `animateFloatAsState` so the bar fills smoothly from zero
  - `CacheSpaceIndicator.kt`: Replaced `Formatter.formatShortFileSize` for max size with enum `.text` property to correctly display "8GB" instead of "8.2 GB" (bypassing base-1000 formatting)
  - `DataSettings.kt`: Passed `showCacheInfo = false` to `CacheSpaceIndicator` to remove duplicate sizing text that was already present in Settings
  - `HomeSongsScreen.kt`: Hoisted `maxCacheSize` preferences to prevent `AnimatedVisibility` from executing a flash animation when the cache size is set to `Unlimited`

Removed:
- remove(home-songs): hide download buttons from On Device tab (issue on-device-downloads)
  - `HomeSongsScreen.kt`: Prevented `download_all` and `delete_downloads` buttons from rendering in the toolbar for `BuiltInPlaylist.OnDevice`
  - `HomeSongsToolbarSettingsDialog.kt`: Removed `download_all` and `delete_downloads` from `tabAvailableIds` for `OnDevice`, so they no longer appear in the settings modal for this tab
