Added:
- feat(listen-together): "Listen Together" — GUEST available (Metrolist host → N-Zik guest), hosting NON-FUNCTIONAL pending server approval (Metrolist port, spec `_bmad-output/implementation-artifacts/spec-listen-together.md`; hosting issue https://github.com/MetrolistGroup/metroserver/issues/6)
   - GUEST (device-verified): join a room (8-char code / invite link), in-place queue sync with the host on every track change (no player restart), host pause/play/seek/volume followed with server-clock drift compensation (EMA-weighted offset + ±2 % speed modulation), kick / ban / suggestion / reconnect events toasted, room management menu (« Listen Together » section under Playback in song / video / player menus, list + grid following the « Menu style » setting, app-pattern forms), username + server choice + « Local history » option persisted
   - HOST: **NON-FUNCTIONAL** — the Meowery server rejects hosting (app user-agent not allowlisted); the 5 host flows (create room, join requests ✓/✕, pending suggestions ✓/✕, user action menu Kick/Block/Transfer, auto-approval toggles) are implemented + unit-tested but not live-verified — verification pending server-side approval (user: « je fais dès qu'ils m'ont approuvé »)
   - Library protection: per-item `listen_together` marker + 4 gates — the host's (Metrolist) queue metadata (literal « Titre » byline, title-as-album, comma-joined channel bylines) is never persisted; the « Local history » LT option gates both the history recording and the verified-metadata flow (`shouldRunStreamInfoUpsert`, aligned with `shouldRecordLocalHistory`): off = LT streams stay fully library-agnostic, on = the track is checked against YouTube through `StreamResolver.upsertSongInfo` (same flow as regular streams) and only the verified data is upserted; user-edited (`modified:`) values are never overwritten
   - Strings: `listen_together_*` keys in `values/strings.xml` only (EN; `values-*/` are Crowdin-managed — no hand edits); protobuf-lite wire format (version catalog, spec-approved); cleartext allowed for localhost/127.0.0.1 only (local LT servers in development)
   - Tests: 51 LT suites green (item marker, in-place queue sync plan, playback sync, bypass ready-wait, history decision, stream-info upsert decision, prepare-after-sync, message codec, server clock, username persistence, join-rejection message) + `DurationIndicatorTimeRemainingOffMainTest` 4; `assembleDebug` BUILD SUCCESSFUL
- feat(rewind): Rewind deck background music — hidden A/B crossfade players, one content track per displayed deck card (issue https://github.com/N-Zik-Group/N-Zik/issues/275)
  - Frozen base: two hidden ExoPlayer instances (no notification, no UI) crossfading on a 400 ms sqrt ramp; the music starts the moment the deck data is ready — under the 6.8 s opening, so the intro has its track too; the card's track loops until the next card takes over; yield to the main player (pause/resume faded 300 ms, never silent); ON by default at 70 %, mute + volume persisted (`DataStoreUtils`), toggle in the Rewind settings card; 400 ms fade-out on deck exit on a dedicated scope (the controller scope is cancelled by then)
  - Content mapping (device-iterated hybrid): the "important" single-item cards always play their #1 (top song, artist spotlight, top album, finale); the list cards roll randomly among the 5 items they display (top artists / top songs / deep cuts / albums — artists with no local match are excluded from the roll, no silent fallback pool); pure `contentTrackForPage(data, page, random)` seedable for tests; the BGM is 100 % anonymous — no recording anywhere (listening stats only go through `PlayerServiceModern.onPlaybackStatsReady`)
  - Stream path: dedicated `StreamResolver.createBackgroundMusicDataSourceFactory()` — same okhttp upstream → resolving data source → LRU stream cache chain incl. the parental gate, but zero metadata maintenance (no `upsertSongInfo` artist/album pre-cache, no `fetchFormatIfMissing`) — the BGM players only read music; the main player keeps its standard factory
  - Device iterations (v4-v6): BGM chrome depop/repop on scroll/settle with the share button's spring (`AnimatedVisibility` + scaleIn/scaleOut), the toggle shares the share button's rounded cream chrome; preload the target card as soon as the swipe starts (`onScrollStarted`, direction from `currentPageOffsetFraction`); fade guard — a standby prepare requested mid-swap-fade is deferred to the fade tail so a `setMediaItem` never cuts the outgoing track; 700 ms crossfade when the track is only ready after the settle (the old track keeps playing until then); audio-focus hand-over at the fade tail only — the standby never requests focus while the outgoing is still fading out (that steal made ExoPlayer pause the outgoing on AUDIOFOCUS_LOSS, killing the crossfade: the device-reported "le fade marche pas au swap")
  - Fast-swipe sync (device iteration v7, 2026-09-25): after a fast flick, the arrival card's settle fell in the 800 ms debounce window and the armed `pendingSwapPage` was never updated → the track preloaded during the swipe sat on the standby unapplied, the music stayed on the previous card until the next swipe. Fix: `currentPage` (the card on screen, updated on EVERY settle incl. skipped) is the single source of truth and `pendingSwapPage` is gone — a standby already READY at settle time swaps immediately (400 ms) even when debounced, an in-flight preloaded track lands on the card on screen as soon as it becomes ready (700 ms), a stale READY (deck moved on) is ignored; the debounce still forbids NEW loads during fast navigation ("si on change trop vite il lit pas")
  - Strings: 3 EN keys `rw_bgmusic_*` in `values/strings.xml` only; tests: `RewindBackgroundMusicTest` 27 (pool build/roll, hybrid mapping incl. prefix-robust artist match, seed determinism, scroll target, fade-tail deferral, standby READY-swap guard, yield state machine, start offsets) — no new dependencies; `assembleDebug` BUILD SUCCESSFUL
  - Verified on device (user, 2026-09-24/25): per-card content matching, music under the opening, depop/repop chrome, no hard cut on fast swipes, the crossfade finally audible on every swap, and the track lands on the arrival card right after a fast flick — "c'est trop beau", "parfait !"
- feat(rewind): N-Zik Rewind — monthly / yearly / global listening recap deck with per-slide share and monthly reminder notification (issue https://github.com/N-Zik-Group/N-Zik/issues/275)
  - Recap is 100 % fed by the existing `Event` journal (read-only, no schema change — DB still v42): windowed SQL in `EventTable` (top songs/artists/albums, total minutes, distinct-song count per window, count-distinct artists/albums/playlists), every query excluding `disliked=1` so a disliked artist/song/album disappears from the recap; `RewindPeriod` sealed model (Month / Year / Global) + pure window-boundary helpers
  - 16-slide deck (`RewindScreen`: `HorizontalPager`, per-slide shaders, scripted 6.8 s opening, mini-player hidden in every captured frame) + home page (`RewindHomeScreen`: one row per year, "MONTHS OF <year>" 12-month grid — empty months greyed and untappable — and the ALL TIME entry, `rememberSaveable` selected year); both driven by ViewModels (`RewindDeckViewModel` / `RewindHomeViewModel`, injected dispatcher, `collectAsStateWithLifecycle()`) so state survives composition and all fetches run on `NzikDispatchers.DATA`
  - Global (all-time) period added on code-review decision (user: « Oui, ajouter le global »): home entry next to years + months, all-time window, same 16-slide deck, « GLOBAL » kicker
  - Sharing (frozen AC #5 + 2026-09-23 user decision « le dernier fait le gros export, et chaque page a son export share système »): the finale keeps the 16-image folder export (SAF, `exportRewindPagesToFolder` — partial-failure cleanup incl. orphaned-document removal, valid persistable permission flags) AND every slide gets its own system share — top-right icon on the other slides, « EXPORT THIS PAGE » pill on the finale — single PNG → `cacheDir/shared_rewind` → FileProvider `<cache-path>` → `ACTION_SEND`
  - On-device capture (user-directed iteration, 2026-09-23): full-bleed frames — the system bars are NOT cropped but hidden with the platform insets transition (`WindowCompat` insetsController), and the copy waits for the hide to land in the framebuffer (`rewindAwaitBarsHidden`: polls the visibility-aware `rootWindowInsets` until status top == 0 AND nav bottom == 0, 16 ms poll / 500 ms timeout) so a frame is never caught mid-fade; the per-slide finale export is pixel-identical to the 16th frame of the full export (footer shown, listener row + export pills + mini-player hidden in every captured frame); the bars stay hidden under the share sheet and come back on the next activity `ON_RESUME` (`showSystemBarsOnNextResume`, one-shot observer) — restoring them right after the copy blinked them back before the sheet covered the screen; on copy/share failure the bars + live chrome are restored immediately
  - Share icon: `AnimatedVisibility` spring pop-in (damping 0.62, `Spring.StiffnessMedium`, initialScale 0.5) + 200 ms fade-in, 160 ms fade-out with scale-down, instant exit during capture (clean frame); 24dp glyph in a 40dp target placed below the status bar
  - `RewindReminderWorker` (frozen task): monthly WorkManager notification "Your <month> rewind is ready" / "Tap to open your monthly rewind." — `CoroutineWorker`, unique work name, ±10 min jitter, self-reschedule in `finally` (internal `rescheduleHook` seam for tests), silent skip (logged, not failed) when `POST_NOTIFICATIONS` is denied on API 33+; "rewind" channel (LOW) in `MainApplication.createNotificationChannels()` + `schedule()` at app startup; contentIntent → `MainActivity` with `EXTRA_DECK_YEAR` / `EXTRA_DECK_MONTH` (the finished month) — cold start in `startApp`, warm start in `onNewIntent`, one-shot `LaunchedEffect` navigation to `rewind?year=..&month=..`, intent parse extracted to `rewindDeckTargetFromIntent` (never re-seeded on instance restoration)
  - Review-driven corrections: listener badge scaled to the period (monthly decks no longer stuck at the bottom tier), spotlight crossfade + wiki fetch gated during capture, « TEN SONGS. ONE WINNER. » copy parameterized to the real count, shared home/deck top-song comparator, dead ported components deleted (`rewind/components/*`), shader list aligned to the 16 pages, capture flags in try/finally, zero-size bitmap guarded
  - Strings: `rw_*` keys added to `values/strings.xml` only (EN; `values-*/` are Crowdin-managed — no hand edits, other locales pick the keys up at the next sync, EN fallback until then)
  - Tests: 15 rewind suites / 61 tests (incl. `RewindDataFetcherTest` 5, `RewindDeckDeepLinkTest` 5, `RewindReminderSchedulingTest` 5, `RewindPeriodBoundariesTest`, period-aware label / ranking / levels / contrast suites) + `EventTableTest` 16/16 (windowed queries + disliked exclusion); Robolectric infra added (`testOptions.unitTests.isIncludeAndroidResources` + `src/test/resources/robolectric.properties` bare-Application override + `@Config(sdk = [33])` on one pre-existing test) — no new dependencies; full module 848 tests, only the 2 pre-existing `LastFmLoginContentTest` failures (identical at baseline `1166e7f3`)
  - Verified on device (user, emulator `5554`, 2026-09-23): deck slides, home (years / months of 2026 / ALL TIME), per-slide share sheet, finale « EXPORT THIS PAGE » frame, 16-image folder export, share icon pop; residual share round-trip flash accepted by the user ("c'est pas grave")
- feat(onboarding): first-launch onboarding flow (permissions → restore → name → accounts) + display name source switchable from Accounts (issue https://github.com/N-Zik-Group/N-Zik/issues/275)
  - 4-step flow rendered in place of `AppNavigation` while the new `onboardingComplete` flag (SharedPreferences via `DataStoreUtils`) is false; the current step is persisted (`KEY_ONBOARDING_STEP`) on every transition, so a process death / post-import restart resumes at the right step; every step is optional — nothing is enforced
  - `OnboardingScreen` (step 1, RiPlay pattern port): app icon, greeting, one card per permission (notifications, media, mic, Bluetooth, battery optimization) with grant/settings actions driven by a pure `PermissionStatus` mapping (PERMANENTLY_DENIED → "Settings"); the set of already-shown system dialogs is persisted (`KEY_ONBOARDING_PERMISSIONS_REQUESTED`) so the mapping survives activity recreation
  - `OnboardingImportScreen` (step 2): optional restore of the database and/or settings from a backup file — the existing `ImportDatabase`/`ImportSettings` pipeline reused as-is (SAF picker, sqlite checkpoint/copy, CSV merge); "Both" chains DB → settings; a successful import writes the flag **before** the restart prompt (`completeOnboarding()`) so the restart lands directly in the app (the restored settings already contain the name / the YT account); "Skip" moves on to the name step
  - `OnboardingNameScreen` (step 3): "Log in (YouTube)" reusing the existing cookie WebView login (`YouTubeLogin`) or "Continue as guest" with an optional custom name (`KEY_USERNAME`)
  - `OnboardingAccountsScreen` (step 4, after the name — user decision 2026-09-23): Last.fm + Discord account cards (permission-card pattern) reusing the existing Accounts-tab logins (`LastFmLoginContent` sheet incl. the keyboard fix, `DiscordLoginAndGetToken` WebView) on the **same encrypted prefs** — onboarding and the Accounts tab observe the same state; a connect only logs in (status card, no screen advance, no restart on the screen); the quit button reads "Skip", then "I'm done" once an account is connected, and the restart prompt is deferred to it with a Discord token set (Rich Presence starts when the player service launches — flag written before the prompt, so the restart lands in the app)
  - Display name resolved from source (pure `resolveDisplayName`): YouTube account name (only when source = youtube + logged in + name captured) → custom name → `display_name_default` ("N-Zik Fan") — consumed by the Rewind deck VM, the intro card, the Accounts card and the home greeting (`WelcomeMessage`, approved legacy edit)
  - `DisplayNameSettingsCard` (n_zik, `LastFmSettingsCard` pattern) inserted at the top of the legacy Accounts tab (approved 2-line insertion in `AccountsSettings.kt`): source switch (YouTube / custom) + custom name field + live preview
  - Legacy `rw_default_username` ("Music Fan") deleted → single `display_name_default` in values/strings.xml + its 45 `values-*/` copies cleaned (user-approved)
  - Review-driven corrections (independent review, FIX-FIRST verdict): navigation intents (`rewindDeckTarget` / `intentUriData`) no longer navigate into an empty NavHost while the gate is up (guarded by `onboardingStep == null` in the effect keys — the target is held until the gate opens, so the monthly rewind reminder notification can't crash a fresh-install launch); duplicated `ytAccountName(context)` reader deleted (GlobalVars helper, explicit non-null `String` return); import options `rememberSaveable` across rotation
  - Strings: `onboard_*` + `display_name_*` keys in `values/strings.xml` only (EN; `values-*/` are Crowdin-managed — no hand edits)
  - Tests: `OnboardingStepTest` (5), `DisplayNameResolutionTest` (8), `OnboardingPermissionStatusTest` (5), `DataStoreUtilsOnboardingTest` (8 Robolectric — flag / step / source round-trips) + `RewindDeckViewModelTest` updated (source resolution + fallbacks); no new dependencies
  - Verified on device (user, emulator `5554`, 2026-09-23/24): fresh install → permissions → restore (skip) → name → accounts → app; connect Last.fm / Discord → status cards, no screen change, no restart; "Skip" with a Discord token → restart → lands in the app; "N-Zik Fan" on slides / greeting / Accounts card
- feat(rewind): Rewind settings — master / per-type (yearly / monthly / global) / per-notification toggles in the AI tab, gated in real time (roadmap item 1, issue https://github.com/N-Zik-Group/N-Zik/issues/275)
  - 6 new `DataStoreUtils` keys, all default `true` on read (zero regression for existing installs): master switch, one toggle per recap type, monthly + yearly notification toggles (yearly one persisted but inert until the yearly worker, roadmap item 2)
  - `RewindSettingsCard` (n_zik, `LastFmSettingsCard` pattern) inserted in the legacy AI tab (user-approved 2-line insertion in `AIRecommendationSettings.kt`): master switch, type toggles aligned with it, notification toggles indented 25.dp under their type with the shared bell icon (device-iterated UI)
  - Hierarchical gate `master && type && notif-type` applied at three points: the burger menu item (user-approved legacy conditional in `ActionBar.kt`, PiP pattern), the Rewind home sections (visibility-only, no fetch change), and the monthly reminder worker (`schedule()` cancels pending work while off, `doWork()` re-checks before the post, self-reschedule step kept)
  - Real-time state for the always-composed app header: new `rememberDataStoreBooleanPreference` (seed + live `OnSharedPreferenceChangeListener` on `app_settings`, `DisposableEffect`, legacy `rememberPreference` pattern) — a flip in the settings hides/shows the burger item and the home sections immediately, no app restart (user device-feedback iteration)
  - Strings `rw_settings_*` in `values/strings.xml` only (EN; `values-*/` are Crowdin-managed — no hand edits)
  - Tests: `DataStoreUtilsRewindTest` (6 Robolectric — defaults / round-trips / real-time listener mechanism) + 4 hierarchical-gate tests in `RewindReminderWorkerTest`; full module 904 tests, only the 2 pre-existing `LastFmLoginContentTest` failures
  - Verified on device (user, 2026-09-24): burger item appears/disappears in real time on the master flip, card hierarchy + shared notification icons, per-type home gating, no monthly notification while the gate is off
  - Review (3 independent layers, 24 findings triaged): 4 patches — master description string no longer promises home entries (frozen contract gates menu + worker only), worker KDoc + skip-branch comment corrected (cadence resumes at next startup while gate on, not "kept"), misleading test name fixed (`...ButStillRunsTheRescheduleStep`), feature logs unified under the `RewindReminder` tag; the red module Compose test harness (pre-existing) is deferred to `deferred-work.md`
- feat(rewind): retire the legacy `monthly:YYYYMM` on-the-fly generation, one-shot cleanup migration and the « Rewind » Playlists-tab category with the YTM-style Month/Year/All filter (issue https://github.com/N-Zik-Group/N-Zik/issues/275)
  - `RewindPlaylists.kt` (new, `core/rewind`): single source of the naming/visibility contract for specs 1 and 2 — prefixes `rewind-monthly:` / `rewind-yearly:` + case-insensitive `isMonthly/isYearly/isRewind(name)` predicates, `rowVisible(monthlyOn, yearlyOn)` (the filter button exists only when BOTH creation toggles are on) and the per-filter content mapping (Month → `rewind-monthly:*`, Year → `rewind-yearly:*`, All → both)
  - `MonthlyPlaylistCleanup.kt` (new, `core/migration`, `RemovedSettingsMigration` pattern): one-shot, idempotent, Timber-logged — SharedPreferences guard; on first run every `name LIKE 'monthly:%'` playlist is deleted via `playlistTable.delete()` (FK CASCADE on `SongPlaylistMap.playlistId` removes the mappings too), ordinary / pinned / YT playlists untouched; wired in `MainApplication.onCreate` right after `RemovedSettingsMigration.run`
  - Legacy generation retired (user-approved whole-file deletion): `MonthlyPlaylists.kt` removed along with its `HomeLibrary` call site and the `enableCreateMonthlyPlaylists` preference read; the dead `PlaylistTable.allMonthlySongs` query removed; the remaining legacy dead branches (PlaylistsMenu / ItemMenu / GridMenu, LocalPlaylistSongs pin gate, PlaylistItem icon, `cleanPrefix`) left in place, harmless
  - Category renamed « Monthly playlists » → **« Rewind »** (user: no « playlists » word needed — the chip lives in the Playlists tab): the existing `rewind` string key (already translated in every `values-*/`) reused across the 4 n_zik surfaces (`HomeLibrary` chip, `HomePlaylistsSettingsDialog` / `HomeLibrarySortSettingsDialog` / `HomeLibraryToolbarSettingsDialog`) + the n_zik settings card title; n_zik identifiers renamed (`monthlyLabel` → `rewindLabel`, chip/toolbar order IDs, enum member `MonthlyPlaylist` → `Rewind`, sort prefix `lib_mon` → `lib_rw`, toolbar ID `mon` → `rw`) — the persisted legacy keys and the legacy `PlaylistsType.MonthlyPlaylist` member stay untouched (one persisted order/visibility reset, accepted)
  - YTM-style filter (HomeAlbum pattern): ONE `FilterChip` showing the current selection (Month / Year / All, `selected = true`), tap → `ListMenu.Menu` with the 3 options each with its own icon (Month → `stat_month`, Year → `stat_year`, All → `musical_notes`, `SettingIcon` pattern); shown only when the « Rewind » chip is selected AND both creation toggles are on — one active type → no button, the tab shows that type directly; none → no button, empty list; selection persisted (new key `"rewindPlaylistsFilter"`, default **Month**), shares the tab's existing sort
  - Frozen settings contract for spec 2 (template, no playlist creation in this spec): 4 new `DataStoreUtils` keys `KEY_REWIND_{MONTHLY,YEARLY}_PLAYLIST[_NOTIF]_ENABLED` (all default `true`, store `app_settings`, KDoc pins the spec-2 wiring); visibility wired from THIS spec — the filter button exists iff both creation toggles are on, live with no restart; the GH-275 deck master / type / notification toggles stay untouched — playlists are decoupled from the deck (user decision 2026-09-24)
  - `RewindSettingsCard` regrouped into 3 n_zik cards (Quick Picks pattern, passes 5-7): « Rewind » (yearly / monthly / all-time type toggles, masked while the master is off), « Rewind notifications » (the 4 notifications together, each masked when its type is off) and « Rewind playlists » (the 2 creation toggles, always visible, decoupled from the master); live reads via `rememberDataStoreBooleanPreference`
  - Legacy `AIRecommendationSettings.kt` restructured (user-approved decision A, re-negotiated passes 3-7): its « Rewind » section removed whole — the master « Enable Rewind » moved into the existing « General » card (live DataStore `KEY_REWIND_ENABLED`) and the rest lives in the 3 n_zik cards just below; the « Quick Picks » and « For You » cards merged into ONE « Quick Picks » card (3 entries + their 2 dialogs, gates `enableQuickPicksPage` / `showTips` kept, `searchCtx_1` rebuilt to absorb the `quick_selection_type` token, `searchCtx_2` deleted); the 2 orphan strings created for it (`show_rewind_in_library`, `enable_rewind_creation`) deleted
  - Strings: the 4 old monthly keys deleted from `values/strings.xml` (`monthly_playlists`, `enable_monthly_playlists_creation`, `show_monthly_playlists_in_quick_picks`, `show_monthly_playlists_in_library`) — reusing them would have kept « Monthly » in every language; new EN keys added (`rw_settings_notifications`, `rw_settings_playlists_title`, `rewind_filter_month`, `rewind_filter_year`)
  - Quick Picks + Android Auto: the « monthly playlists » section / toggle removed (`HomeQuickPicks`, `HomeQuickPicksSections`, `QuickPicksContentSettingsDialog`) and the automotive « Monthly » folder / IDs / queues removed (`AutoSessionConstants`, `PlaylistsBrowseHandler`, `AutoSessionCallback`)
  - UI rework passes 5-9 (user device iterations, no diff/review layers per user): card show/hide animation made GLOBAL in the `SettingsSectionCard` helper — new `visible: Boolean = true` param (default keeps every other screen untouched), `expandVertically` / `shrinkVertically` (Compose 1.12: `collapseVertically` was renamed) + fade at 400 ms in / 200 ms out, the height animates to 0 so siblings slide without a layout jump; all 7 AI-screen cards converted (search-context + master gates kept) and their animation imports / `@ExperimentalAnimationApi` dropped; `SettingsCardTransitions.kt` (new) holds the shared `settingsEntryEnter` / `settingsEntryExit` specs now used by every n_zik settings card (Rewind, LastFm inner scrobbling blocks); the no-op `AnimatedVisibility(visible = true)` wrappers removed from `LastFmSettingsCard` / `DisplayNameSettingsCard` (AnimatedVisibility does not animate on first composition); the « Edit custom name » bottom sheet rebuilt on the Last.fm login sheet pattern — transparent container, bottom corners 0, `ListMenu.Menu(title = display_name_edit)` chrome (themed handle bar + title + divider + background0 + height cap) inside a 50 %-screen Box, field / Save button with the `LastFmLoginContent` paddings; `display_name_description` corrected « Name shown on the Rewind slides » → « Name shown on the app » (+ 4 KDoc / comment alignments)
  - `values-*/` cleanup (user-approved exception to the Crowdin no-hand-edit rule): the 4 deleted monthly keys removed from all 49 `values-*/strings.xml` (188 lines; the 4 old-format files pt-rBR / sr-rCS / zh-rCN / zh-rTW only held 2 of the 4) — CRLF + BOM-less UTF-8 preserved, 0 occurrence left anywhere in `res/`
  - Tests: `RewindPlaylistsTest` (10 — predicates / casing / visibility combos / per-filter content) + `MonthlyPlaylistCleanupTest` (3 Robolectric + Room — first-run delete incl. mappings, ordinary playlist intact, second run no-op); no new dependencies; module suite green, `assembleDebug` BUILD SUCCESSFUL
  - Verified on device (user, 2026-09-24): « Rewind » chip, the YTM filter button + 3-option menu, card / entry animations, the settings card hierarchy, the rebuilt « Edit custom name » sheet
- feat(rewind): auto-generation of Rewind playlists — monthly / yearly / all-time workers from the listening journal + protected generated playlists (issue https://github.com/N-Zik-Group/N-Zik/issues/275)
  - `RewindPlaylistGeneration.kt` (new, `core/rewind`): single generation path for every `rewind-*` playlist — the period's most-played songs (`findSongsMostPlayedBetween`, playtime DESC — frozen contract), playlist create-or-update + full song-mapping replace, the "Rewind Top" order snapshot persisted in `SongPlaylistMap` so the deck order survives any user re-sort, skip-if-exists dedup on the language-neutral name
  - `RewindMonthlyPlaylistWorker` + `RewindYearlyPlaylistWorker` (new, `core/rewind`): `CoroutineWorker`, unique KEEP work, delay until the next local 1st of month / 1st of January 00:00 + positive-only jitter (never before the period end), spec-1 gate re-check at execution (`KEY_REWIND_{MONTHLY,YEARLY}_PLAYLIST_ENABLED`), self-reschedule in `finally`; monthly covers the just-finished month, yearly the just-finished year; `RewindPostImportRegenerationWorker`: one-shot recompute of every existing `rewind-*` playlist after a database import (wired in `ImportDatabase` / `ImportMigration`)
  - Period lock (user decision 2026-09-24 v2-v6): unfinished periods never open — the in-progress + future months and the in-progress year are locked on the Rewind home, access granted by DATE (1st of the next period, 00:00 local — the same instant the notification fires, never on notification delivery: force-stop / denied permission / failed worker must not lock the user out); `isMonthComplete` / `isYearComplete` in `RewindPeriod.kt`; ALL TIME never locked
  - Locked look (user visual iterations v3-v6): locked month cell + locked year row = the N-Zik launcher box under the same dark 0.7 scrim as the playable cells (full opacity, padlock, "LOCKED" under the month label / instead of the stats, no album previews); empty finished months get the same background + "You didn't play anything, that's sad."; the cells'/rows' base background `Color.Black` → `palette.background1` (no black flash while network artworks load)
  - Spammer easter eggs: hammering a locked cell / year row answers with a 5-message funny rotation (the kreate in-app `Toaster`: `i` for the rotation, `e` red for the kick), the 6th tap → "Enough! I said NO — stop!" + `popBackStack()` out of the home (`nextLockedSpamReply`, pure + tested); tapping an empty finished month answers with a random sad one-liner among 6 strings, no kick (`randomEmptyMonthMessage`, pure + tested)
  - Generated playlists protected (Spotify principle): no rename, no pin, never an add-to target in the 5 menus, no renumber / position-lock; "Rewind Top" sort option shown and default-forced only for `rewind-*`; localized display names on every surface without touching the language-neutral DB name; the Quick Picks "Rewind" section and the automotive display names restored
  - Deck exclusion (user decision 2026-09-24): `findPlaylistListeningStatsBetween` excludes `rewind-%` playlists so generated recaps never pollute the listening stats (SQLite LIKE is case-insensitive over ASCII — matches the name contract)
  - Legacy edits (user-approved, the spec's 6 files): `LocalPlaylistSongs.kt` (isRewind gate, Rewind Top default, rename / pin guards), `LocalPlaylistItemMenu.kt` rename guard, `PlaylistsMenu.kt` / `PlaylistsItemMenu.kt` / `PlaylistsItemGridMenu.kt` / `PlayerMenu.kt` add-to guards, `PlaylistSongSortBy.RewindTop` + `PlaylistItem.kt` origin icons / display names; n_zik: `Sort.kt` `getEnumConstants` made `protected open` for the "Rewind Top" filter override in `PlaylistSongsSort`
  - Strings: 21 EN keys in `values/strings.xml` only (incl. `rw_home_locked`, `rw_home_locked_spam_*`, `rw_home_month_empty_sad`, `rw_home_month_empty_spam_*`; `values-*/` are Crowdin-managed — no hand edits)
  - Tests: `RewindPlaylistGenerationTest` (16), `RewindPlaylistWorkersTest` (17), `RewindPlaylistsTest` (10), `EventTableTest` (17 — incl. the deck exclusion), `RewindPeriodCompletionTest` (5), `RewindLockedSpamTest` (4), `RewindEmptyMonthSpamTest` (4); all rewind suites green
Fixed:
- fix(actionbar): reactive "DEBUG" badge in the header + width-capped marquee labels for the header badges and the burger "Enable debug logs" item (user-reported, no issue — 2026-09-25)
  - Bug: the inline debug badge in legacy `AppTitle.kt` was static — always shown, tap = toast + settings navigation only, no state; replaced by `DebugLogsBadge` (new, `app.n_zik.android.components.ui.header`): live `rememberPreference(logDebugEnabledKey)`, hidden while off, tap toggles with the burger item's exact side effects (restart-required toast on enable, `purgeDebugLogs` on disable)
  - New `HeaderVersionBadge` (BuildConfig/`Updater.extractVersionSuffix` logic moved out of legacy `AppTitle.kt`): the version badge now shares the `HeaderBadgeBox` container with the debug badge
  - `HeaderBadgeBox` (shared container): one line, width capped at the rendered "DEBUG" badge width (measured live via `rememberTextMeasurer` — new shared `clampedLabelMaxWidth` helper), longer labels marquee FINITELY (`basicMarquee(iterations = 3)`: a few full passes then the scroll stops and snaps back to start — user request), 14 sp bold = the "Rescue Center" burger item size (labelLarge, max text size); `clickable` applied only to the interactive debug badge (even a disabled `clickable` enforces the 48 dp touch target and would inflate the non-interactive badges)
  - Burger item `DebugLogsMenuItem`: the "Enable debug logs" label is REACTIVE — "Enable debug logs" off / "Disable debug logs" on, flips live without restart (new `disable_log_debug` string in `values/strings.xml`; `values-*/` are Crowdin-managed — no hand edits); one line, clamped to the rendered "Rescue Center" label width (same `clampedLabelMaxWidth` cap — no burger label may be wider than "Rescue Center"), marquee when longer (`Int.MAX_VALUE`, project convention; honors the global `disableScrollingTextKey` setting, ellipsis fallback)
  - Legacy diff minimal (user-approved): `AppTitle.kt` — the two inline badges replaced by `DebugLogsBadge()` + `HeaderVersionBadge()`, orphan imports cleaned; `Utils.kt` — the now-orphan `Preference.debugLog()` + its import removed
  - Tests: `DebugLogsBadgeTest` (5 Compose/Robolectric — shown when on / hidden when off / tap toggle + log purge / live preference follow / clamp in a tight header) + `HeaderVersionBadgeTest` (3 — long label clamped to the "DEBUG" width, short label compact, longest real label "MINIFIED 32" clamped) + `DebugLogsMenuItemTest` (3 — enable label when off, live label flip, tap toggle + purge + label flip); 11/11 green; `assembleDebug` BUILD SUCCESSFUL
  - Test-environment notes: compose-ui-test resolves to 1.12.0 (catalog says 1.11.4) — `fetchSemanticsNode().size` / `positionInRoot` / `assertWidthIsEqualTo(Dp)`; `BasicMarquee` is draw-only so its scroll is not observable in unit tests (the clamp is); Robolectric's text measurement is degenerate (width ≈ char count) and `combinedClickable` merges descendant semantics (the standard material3 `DropdownMenuItem` does the same — verified in source), so the burger label's own clamp is not observable through its node — the mechanism is shared with the tested badge container
- fix(rescue): kill the main app process automatically every time the Rescue Center opens
  - Root cause: rescue write actions (e.g. the database export) refuse to run while the main process is alive, so the user had to go to Android Settings > Force stop before any rescue write could succeed
  - New `RescueProcess` object in `core/rescue`: `requestKillMain(context)` sends an intra-app broadcast (`ACTION_KILL_MAIN`, always `setPackage` to the app's own package) AND writes a safety-net flag file (`rescue_kill_main.flag` in filesDir) — fire-and-forget: a failed broadcast is only logged, the flag still covers the request, and vice versa
  - `MainApplication.kt` `onCreate`: right after the `isMainProcess()` guard and BEFORE any heavy init, `consumeKillRequest` is checked — if a kill request is pending the process ends itself (`Process.killProcess(myPid)`, the public API; the framework's `exitProcess` is @hide); `registerKillMainReceiver()` is then registered early (before `Dependencies.init`) on the main process only
  - The kill receiver runs on `RescueProcess.KILL_REQUEST_HANDLER`, a dedicated daemon `HandlerThread` ("nzik-rescue-kill"): a manifest receiver or a handler-less one would be invoked on the main thread — precisely the thread frozen during an ANR — so the background looper can receive the broadcast while the main thread is stuck; `RECEIVER_NOT_EXPORTED` from API 33 (setPackage-only below) keeps the broadcast invisible outside the app
  - `RescueActivity.kt`: `onCreate` calls `RescueProcess.requestKillMain(this)`, so both entry points (launcher shortcut and in-app burger menu) trigger the kill
  - `RescueScreen.kt`: while a request is pending, a status poll (`LaunchedEffect(killRequestGeneration)`, 1 s cadence on `RescueFiles.isMainProcessRunning`) reports "stopping" then "stopped" and calls `cancelKillRequest` as soon as the death is observed (a stale request must never self-kill the next healthy launch); manual "Kill the app" `RescueActionCard` added in the Danger Zone to re-send the request after a failed kill; `guardWrite` untouched
  - `strings.xml`: 2 adjusted keys (`rescue_main_process_running`, `rescue_error_database_busy`) + 5 new keys (`rescue_status_main_process_stopping`, `rescue_status_main_process_stopped`, `rescue_kill_app`, `rescue_kill_app_description`, `rescue_confirm_kill_app`)
  - New `RescueProcessTest.kt` (9 JUnit5 tests: flag write/consume/idempotence/overwrite/cancel/parking-file) and `RescueProcessRequestTest.kt` (2 Robolectric tests: the broadcast reaches the intra-app receiver with the right action/package, and a failed kill can be re-sent) — 58/58 package tests pass
  - Deviations: `Process.exitProcess` is @hide → `Process.killProcess(myPid)`; public Android has no executor-based `registerReceiver` → the receiver is bound to a `HandlerThread`
- fix(lastfm): move login sheet above keyboard on IME open
  - Root cause: the sheet rested in the partially expanded state whose top anchor is a fixed 50% of the window height; on API 30+ the sheet dialog window uses SOFT_INPUT_ADJUST_NOTHING, so the keyboard never resized it and the IME insets only grew the content downward, leaving the username/password fields hidden behind the keyboard
  - `LastFmSettingsCard.kt`: sheetState now uses `rememberModalBottomSheetState(skipPartiallyExpanded = true)`, so the sheet opens fully expanded where the anchor is content-driven (fullHeight - sheetHeight) and follows the IME insets — same behavior as the working YouTube login sheet
  - `LastFmSettingsCard.kt`: sheet content wrapped in a `Box` capped at ~50% of `screenHeightDp` (`heightIn(max = ...)`) to keep the current half-screen appearance
  - `LastFmSettingsCard.kt`: login button now disabled until both username and password are non-blank
  - `LastFmSettingsCard.kt`: `LastFmLoginContent` made `internal` (was `private`) for testability
  - New `LastFmLoginContentTest.kt`: Compose UI test (`createComposeRule`, JUnit 4/Robolectric) verifying both fields render and the button disabled/enabled states
  - `libs.versions.toml` + `ComposeN-Zik/build.gradle.kts`: added `compose-ui-test` ref (`androidx.compose.ui:ui-test-junit4`, version aligned with `compose` 1.11.4) as a test-only dependency
- feat(rewind): yearly reminder worker — Jan 1 notification (roadmap item 2)
  - `RewindYearlyReminderWorker` (new, same pattern as `RewindReminderWorker`): internal `CoroutineWorker` + `schedule(context)` — gate off → `cancelUniqueWork`, else unique KEEP `OneTimeWorkRequest`, delay = ms until next local Jan 1 00:00 + positive-only jitter up to +10 min (the notification can never fire before the New Year), self-reschedule in `finally`, gate re-check at execution, skip with log when `POST_NOTIFICATIONS` is denied (API 33+)
  - Hierarchical gate frozen by GH-275: `KEY_REWIND_ENABLED && KEY_REWIND_YEARLY_ENABLED && KEY_REWIND_YEARLY_NOTIF_ENABLED` (DataStore `app_settings`, all default true) — the yearly toggle (`rewind_yearly_notif_enabled`) was stored but inert, this is the last point of the GH-275 contract
  - Notification: reuses the existing `rewind` channel (IMPORTANCE_LOW), stable ID `0x7265776C` distinct from the monthly `0x72657769`, title reuses `rw_notif_title` with the finished year, new body string `rw_yearly_notif_body` in `values/strings.xml` (EN only); `rw_notif_body` renamed `rw_monthly_notif_body` for symmetry (2 refs, no test pins, no `values-*/` translations to update)
  - Content intent → existing `rewind?year=<finished year>` route (finished year = `LocalDate.now().year - 1` computed at execution; month extra absent/`0` is the yearly-deck sentinel — the monthly contract is unchanged); intent decode and navigation extracted into testable `rewindDeckTargetFromIntent` + top-level `rewindDeckRoute(year, month)` in `MainActivity`
  - Wiring: `RewindYearlyReminderWorker.schedule(this)` in `MainApplication.onCreate`
  - Tests: new `RewindYearlyReminderWorkerTest` (9 — gate hierarchy, year notification, producer/consumer join on the shared `EXTRA_DECK_YEAR`, channel/priority/autoCancel asserts, Europe/Paris zone) + 3 new `RewindDeckDeepLinkTest` cases (missing year, yearly and monthly route forms, 11/11); final suite 919 tests / 0 failures
  - `RewindReminderWorker` (monthly): the same positive-only jitter fix as the yearly one — the previous symmetric `(-J..+J)` draw could advance the fire up to 10 min BEFORE the month boundary → previous-month deck (user-approved renegotiation of the frozen "no monthly worker changes", jitter fix only)
  - Compose test harness repair (user request, test-only): Robolectric's unit-test sandbox resolves activities against the app's packaged DEBUG manifest, not the unit-test merged manifest — declaring `androidx.activity.ComponentActivity` in `src/debug/AndroidManifest.xml` (debug build type only, never in release, no-op unexported activity) lets `createComposeRule()` resolve its default activity; both `LastFmLoginContentTest` tests that failed at baseline now pass → baseline fully green (919/919)
- fix(smart-shuffle): restore related-songs recommendations — "Related" tab resolved by its stable MPTR browseId prefix instead of the hard-coded tab index (user-reported, no issue — 2026-09-24)
  - Root cause 1 (all locales, incl. the user's English device): YouTube inserted a "Lyrics" tab at index 1 of the `next()` response, moving the related playlist tab (`MPTRt_…`) from index 2 to index 3 — the hard-coded `tabs.getOrNull(2)` in `RelatedSongs.kt` now hit the "Comments" tab (browseId = null) → zero recommendations, silently; proven by a live locale matrix (en/FR, fr/US, en/US, fr/FR: 4 tabs, MPTR at index 3 in all) and the user's logcat (8× `next()` 200, zero `browse()` calls)
  - New `findSimilarTabBrowseId(tabs)` (internal, `RelatedSongs.kt`): first tab whose browseId starts with "MPTR" — robust to tab order; plus `hl="en"` pinned on the MPTR `browse()` via the existing isolated-session path (shared session untouched) so the "You might also like" shelf title is never localized — root cause 2, proven: fr → « Découvrez également » (exact-match miss) vs en → hit, 20 tracks in both
  - `HomeSongs.kt` (caller): silent `?.getOrNull()` + `catch { continue }` replaced with Timber (tag `SmartShuffle`, `private const val TAG`): cancellation → `w`, failure → `e(cause)`, empty result → `w` (success branch only, so it never masks the failure line), other exceptions → `e` + continue; `CancellationException` rethrown in the catch so a cancelled `LaunchedEffect` stops instead of logging one line per remaining seed
  - Reviewer-exposed bug (user device report: orange « Unmatched song » icons + match button appearing when Smart Shuffle is ON): recommended explicit songs were built `id = "e:" + videoId` (13 chars) with an unprefixed title, breaking the app convention (`Innertube.SongItem.asSong`: raw videoId, `e:` on the title) — triggered the `id.length != 11` "unmatched" icon + toolbar match button, bypassed the parental-control filter (it checks the title) and defeated id-based dedup; fixed in both consumers — `HomeSongs.kt` and legacy `LocalPlaylistSongs.kt` (user-approved legacy edit, 2026-09-24)
  - Tests: `RelatedSongsTabSelectionTest` (6 JUnit5, no network): new tab order (MPTR@3), legacy order (MPTR@2), no MPTR tab → null, empty list → null, "MPTR" at a non-leading position ignored (pins `startsWith` over `contains`), first MPTR wins when several
  - Review (3 independent layers + user report, 19 findings triaged): 7 patched (cancellation rethrow, empty-warn on success only, explicit id/title in both consumers, 2 contract-pinning tests, TAG const, KDoc), 5 deferred to `deferred-work.md` (incl. `RelatedPage.kt` Quick picks carrying the same latent index-2 tab fallback behind its own hl pin), 3 rejected (out of the frozen logging contract / spec-fixed string / no demonstrated harm)
  - Verified: `:oldtube:test` 6/6 + module suite green, `:ComposeN-Zik:test` green, `:ComposeN-Zik:assembleDebug` BUILD SUCCESSFUL; on device (user, 2026-09-24) pre-id-fix the recommendations flow again (that check surfaced the unmatched-icons report); post-id-fix device check = the commit-approval confirmation
Improved:
- improve(playlists): batch playlist-membership indicator state — one indexed DB query per screen instead of one reactive query per song
  - New `PlaylistStateManager.getPlaylistStates(songIds): Flow<Map<String, Boolean>>` in `core/database`, mirrors `LikeStateManager`; explicit `false` materialized for every requested ID (join-table semantics), empty list short-circuits to `emptyMap()` before the DAO call
  - New `@Query` `songsInPlaylists(songIds)` (`SELECT DISTINCT songId ... IN (:songIds)`) on `SongPlaylistMapTable`, served by the existing index on `songId`
  - `SongItem` gains an optional `inPlaylist: Boolean?` param: precomputed value used when provided, otherwise the existing per-song `isMapped` fallback is kept (menus, podcasts, online search, widgets, legacy `SongItem`, player action bar — all untouched)
  - Wired into all 24 screens already passing `isLiked` (18 files: 8 new package + 10 legacy, additive-only), one `collectAsStateWithLifecycle(emptyMap(), context = NzikDispatchers.DATA)` batch collect per screen with the same `remember(ids)` key as the like map
  - New `PlaylistStateManagerTest` (4 tests, JUnit 4/Robolectric): mapped/unmapped materialization, unknown ID → false, empty list → empty map, reactive re-emission on playlist insert
  - Verification: full `testDebugUnitTest` run = 781 tests, the 4 new ones green; the only 2 failures are pre-existing `LastFmLoginContentTest` Compose UI failures (`Resources$NotFoundException`, from the lastfm commit, unrelated to this change)

Fixed:
- fix(onboarding): single generic log in/log off labels + reactive YouTube onboarding button
  - Replaced the 7 per-service connect/disconnect string keys (`lastfm_connect/disconnect`, `discord_connect/disconnect`, `youtube_connect/disconnect`, `onboard_name_youtube_button`) with one generic pair `account_login`/`account_logoff` in `values/strings.xml` — connect/disconnect states can no longer read differently per service or drift between locales
  - Deleted the 7 orphans from all 49 `values-*/strings.xml` (explicit user instruction; one-off override of the values-* no-edit rule — the keys are also gone from the Crowdin source of truth)
  - New pure mapping `onboardingAccountButtonResId(isConnected)` in `OnboardingAccountButton.kt`, shared by the Last.fm/Discord onboarding cards (`OnboardingAccountsScreen.kt`), the Accounts tab entries (`LastFmSettingsCard.kt` + legacy `AccountsSettings.kt`, 5 refs) and the YouTube onboarding card
  - The YouTube onboarding button in `OnboardingNameScreen.kt` is now reactive (`remember { mutableStateOf(isYouTubeLoggedIn()) }`) — it previously read the login state once and showed "Log in" permanently; it now shows "Log off" when logged in, and clicking it logs the account out (clears the 7 encrypted YT prefs, `cookieStatus = NOT_LOGGED_IN`, resets `Innertube.useLoginForBrowse`, bumps `encryptedPreferencesUpdateTrigger`) — minimal mirror of the Accounts tab logout, no destructive synced-data clear since onboarding is a fresh install
  - Tests: `OnboardingAccountButtonTest` (3 tests) green
- fix(onboarding): a successful restore no longer completes the onboarding
  - Root cause: after any successful import from the onboarding restore step (database / settings / both), the activity wrote `onboarding_complete=true` before the restart (`completeOnboarding` via the `onRestoreDone` callback), so the post-import restart bypassed the whole flow and landed in the main app
  - `OnboardingImportScreen` no longer takes an `onRestoreDone` callback: a successful import advances the flow to the next step (persisted) and triggers the restart prompt — the complete flag stays unwritten, so the restart lands on the next step and the user stays inside onboarding with the restored data live after the preferences re-read
  - The name step auto-skips when a YouTube account is already connected on first composition (pure `nameStepInitialAction`, unit-tested in `OnboardingNameStepActionTest`): the restored display-name source (youtube or custom) is kept as-is instead of the step's "Continue" button silently switching it to `custom`; changing the name stays possible later (Accounts tab)
  - Same auto-skip on the accounts step when BOTH Last.fm and Discord are already connected on first composition (pure `accountsStepInitialAction`, unit-tested in `OnboardingAccountsStepActionTest`): the step is skipped the way the skip button would in that state — flag written + restart prompt (a Discord token is only live after a restart)
  - The complete flag is now written only when the flow fully completes or when the accounts step leaves with a Discord token set (that path is unchanged)
  - Extracted the startup step resolution into a pure `OnboardingStep.resolveStartupStep(complete, persistedStepName)` used by `MainActivity.onCreate`; `OnboardingStepTest` gains 4 tests incl. the regression guard "post-import restart resumes in onboarding, never in the app", plus `OnboardingNameStepActionTest` (2) and `OnboardingAccountsStepActionTest` (4) for the name/accounts auto-skips
- fix(rewind): artist spotlight bio reads the stored in-app description, no by-name web fetch (the "E.T" spotlight rendered the wrong same-named article, user-reported, no issue — 2026-09-25)
  - Root cause: the top-artist spotlight card resolved its "about" text with a by-name lookup, so a channel-derived artist row whose name collided with an unrelated entity (the "E.T" case — confirmed against a DB export) rendered that other entity's article
  - New `RewindArtistBioResolver` (n_zik, pure + injectable `fetchPage`): the stored in-app description is the sole first-class source — a non-blank stored description short-circuits ANY network fetch (the test stubs the fetch to throw, proving no network ran); only when the stored description is blank does it fetch the Innertube artist page by browseId (trimmed); missing browseId / failed fetch / blank page description → null, so the card renders without an "about" section instead of a wrong one
  - `RewindTopArtistSpotlightCard.kt` + `RewindStoryComponents.kt`: bio wired to the resolver, the by-name fetch path is gone
  - Tests: `RewindArtistBioResolverTest` (6 — stored-wins no-network, blank-stored fallback to page, browseId trim, missing-stored+browse → null without fetch, failed fetch → null, blank page description → null)
- fix(database): reconcile the artist↔song mapping on every insert/upsert from a complete author list — stops the add-only pollution at the source (the "E.T" Rewind top-artist bug, device-verified)
  - Root cause: `SongArtistMap` accumulated the union of every author list ever seen (insert-only); YTM returns different author lists per playback context (a channel playlist / radio credits the channel as an "author"), so a channel-derived row collected plays of hundreds of unrelated songs and ranked as a Rewind top artist; extra writers fed it (the `Player.kt` echo loop re-injecting DB mappings into the live `MediaItem`, and the second add-only path in `upsert`)
  - New `ArtistMappingReconcile` (pure, testable): `isCompleteAuthorList(parsedNames, authors)` — a list may only REPLACE existing rows when non-empty and complete, i.e. every parsed name is backed by an author entry carrying a browse ID; names-only / partial lists never erase mappings they cannot rewrite
  - `Database.kt`: `upsert(songItem)` reconciles (drop the song's rows, re-upsert the artists, re-map) when the author list is complete, else keeps the legacy add-only `mapIgnore`; `insertIgnore` reconciles on BOTH the ids path and the names-only path, guarded by `dbSong != null` (only an existing song has stale rows to drop — a fresh insert never wipes)
  - Timber `RECONCILE song=… dropped=N latestList=…` under the `Database` tag
  - Tests: `ArtistMappingReconcileTest` (6) + `DatabaseInsertIgnoreReconcileTest` (4); verified on device (user: "ça marche")
- fix(migration): launch-time cleanup of polluted artist↔song links — idempotent, re-runs on every launch, no flag (heals upgraded + re-imported databases, device-verified)
  - `DbCleanup.kt` (new, `core/migration`, `MonthlyPlaylistCleanup` pattern): a (song, artist) link is stale when the artist name does not appear in the song's stored artist list; custom values untouchable — a `modified:` artist name or a `modified:` song artist text is never judged, and a blank artist text / blank artist name / missing artist row keeps the link; runs on `NzikDispatchers.DATA`, never the main thread
  - No flag, re-runs on EVERY launch (deliberate): the original one-shot flag ran once on a clean DB and then blocked every later run — including right after a polluted re-import, exactly when it is needed; idempotent (zero deletions on a clean DB, silent) and a failed run self-heals on the next launch
  - `SongArtistMapTable.kt`: +`allPairsDirect()` (full pair scan) + `deletePairDirect(songId, artistId)` (single-row delete); the cleanup only ever touches `SongArtistMap` — the `Song` / `Artist` tables are read-only
  - Wired in `MainApplication.onCreate` right after `MonthlyPlaylistCleanup.run`; schedules `RewindPostImportRegenerationWorker` when links are removed (the mapping feeds the Rewind deck); toast "Removed %d incorrect artist link(s)" only when > 0 — `db_artist_link_cleanup_toast` in `values/strings.xml` (EN only), formatted with `context.getString(...)` and passed to `Toaster.s` as a plain `String` (an `Int` count would bind to the `duration` param of the no-vararg `Toaster.s(Int, Int)` overload, leaving `%d` unfilled → `MissingFormatArgumentException` on device); a toast failure is caught + logged separately from the cleanup
  - Tests: `DbCleanupTest` (11 — decision rule: stale / kept / case-insensitive / `modified:` guards / blank guards; orchestration: empty DB, clean DB, stale removed while real kept, missing-song pair kept); module suite green, `assembleDebug` BUILD SUCCESSFUL
- fix(artist): one YTM author entry = one artist — channel-anchored mapping, whole-name display on every surface, phrase-match startup sweep (user-reported "Bigflo, Oli" split, device-verified 2026-09-26)
  - Root cause (device-captured): `parseArtists()` split a single author entry ("Bigflo & OLi" → "Bigflo" + "OLi"), each fragment losing the channel browseId → the upsert judged the author list incomplete → ADD-ONLY fallback + one online search per fragment → junk rows ("Bigflo", "OLi" carrying the first search result's channel ID) re-created on every play (12:18 capture: `added=2 → dropped=2 → added=2` in 500 ms); the same split also leaked into every display conversion (`asSong` / `asMediaItem` / album rows / home / search), so the app showed "two artists" where YTM credits one channel
  - `Database.kt` `upsert` re-anchored on author entries: 1 entry with browseId = 1 artist = 1 row; the row name is owned by the channel page (an existing row keeps its stored name — never clobbered by the raw entry name, `modified:` respected; a new row starts from the entry name and converges to the page name at the next background fetch); `artistsText` := join of the resolved row names; the author list is now complete → RECONCILE path (no per-fragment online search, no junk rows); name-only entries keep the legacy path unchanged
  - `ArtistMappingReconcile.isCompleteAuthorList` compares normalized names (trim + NBSP) — YTM raw entry names can carry no-break spaces
  - `insertIgnore` (both branches) aligns `artistsText` := the join of the names of the rows the mapping actually points to (ids branch: read-back `findByIdDirect(id)?.name` after the no-op-tolerant `insertIgnore`; names-only branch: context names) with the `retainIfModified` guard — one source of truth for the mapping and the display, which breaks the "map on play → deleted on restart" loop
  - Startup sweep `DbCleanup.isSuspiciousArtistLink`: the artist name must match the song's `artistsText` as a whole phrase `(?<!\p{L})<escaped>(?!\p{L})` (Unicode letters, ignoreCase, no `\b` — CJK has no word boundaries) — "COOL&CREATE", "Grand Corps Malade, Kimberose" and JP/romaji variants are kept, true junk (E.T., shredded fragments) is still removed
  - Legacy `splitArtistNames` (approved `Utils.kt` exception): the localized conjunction word now matches as a whole word (`\b...\b`) — "Grand Corps Malade" is no longer shredded into "G" / "r" / "Corps Malade" in the "More of" menus, the info screen and the import; "&" / "," delimiters stay boundary-less
  - New n_zik helper `ArtistEntryNames.artistEntryNames()` (1 entry = 1 whole name, same entry rules as the upsert) consumed by every remaining `parseArtists().joinToString(", ")` display/write site — 20 lines / 15 files: legacy `Utils.kt` (`SongItem.asSong` / `VideoItem.asSong` / `asMediaItem` ×2 `setArtist` + extras `artistNames` — the player/queue strip, and the name↔id zip is now aligned), `AlbumItem.asAlbum`, album writers (`Database.kt` ×2, `StreamResolver.kt` ×2, `UpdateYoutubeEntity.kt`), album page (`AlbumScreen`, `OnlineAlbumItemMenu`), home/quick picks (`HomeSongs`, `HomeQuickPicksSections`, `HomeQuickPicksComponents`), online search (`OnlineSearchList`, `OnlineSearchGrid`), local playlists (`LocalPlaylistSongs`), artist videos tab (`ArtistVideos`), automotive (`ArtistDetailHandler` ×2, `AlbumDetailHandler`, `SearchBrowseHandler`); the 7 legacy UI files are an explicit user-approved exception; zero functional `parseArtists()` call sites left in `androidMain`
  - `modified:` names and artist texts are never touched anywhere; no schema change, no new dependency, no user-facing string
  - Tests: `DatabaseInsertIgnoreReconcileTest` 13, `DbCleanupTest` 20, `DatabaseUpsertChannelIdentityTest` 6 (new), `ArtistMappingReconcileTest` 6, `SplitArtistNamesOffMainTest` 7 (new), `AsSongEntryNamesOffMainTest` 6 (new), `AsMediaItemEntryNamesOffMainTest` 5 (new, Robolectric); full suite green except the pre-existing flaky `RescueScreenProcessStatusTest`; `assembleDebug` BUILD SUCCESSFUL
  - Device-verified (user, emulator, 2026-09-26): DB dump — 32/33 Bigflo songs `artistsText="Bigflo & Oli"`, all 33 mapped to the single channel row, 0 junk rows; artist page Top songs render "Bigflo & OLi" (UI dump); player/queue, album, home and search render whole names; logcat — RECONCILE `dropped=1 → re-link ×1` per play then stable, `DbCleanup` removes 0 stale links at restart
  - Residuals (known, non-regressions): the stale song row 5laPx23QKJU self-heals at its next replay; the orphaned pre-fix junk rows are deferred to a future cleanup; YTM itself serves the channel name per request locale ("Giolì & Assia" in fr/it vs "Giolì, Assia" in en) — the app stays faithful to its own request locale (user-accepted, no locale forcing)
  - Verified on device (user, emulator `5554`, 2026-09-25): vs the 2026-09-20 export, 1913 links removed — independent per-link re-evaluation = 0 false positives (1902 rule-removed genuinely stale, the 11 "unexplained" are import song-deletion cascades, not the cleanup); `Song` / `Artist` untouched by the cleanup (the 15 songs / 11 artists / 9 artistsText drifts belong to the import, not the cleanup); top offenders `E.T` 352/352, `Akeos` 64, `E.T.` 60, `insaneintherainmusic` 57, `Trevor Alan Gomes` 57, `Hatsune Miku` 44, `RoughSketch ` 43, `CosMo` 24; ~20 residual stale links (~0.1 %) re-cleaned on the next launch; "Hatsune Miku" case: 44 romanized-row links removed from songs credited `初音ミク` — 37 still crediting a matching row, 7 transiently unlinked and re-linked on replay (MikuFiesta confirmed in DB → `AlexTrip Sands, Hatsune Miku`), no music lost
- fix(album): album details content extends under the transparent system nav bar — like every other page (user-reported, no issue — 2026-09-26)
  - Bug: on the album details page the list stopped above the system navigation bar and an opaque background0 strip was visible behind the Android nav buttons; on every other page (Skeleton-based: artist, home, search, …) the list runs edge-to-edge under the transparent nav bar
  - Root cause: the active album screen’s root `Box` (`app.n_zik.android.components.ui.screens.album.AlbumScreen.kt`, routed via `AppNavigation`) applied `.navigationBarsPadding()` to the whole page — unlike the `Skeleton` content area (`padding(bottom = 0.dp)`), it shrank every descendant above the system nav bar inset
  - Fix: removed the root `.navigationBarsPadding()` (one line) — the `LazyColumn` now reaches the screen bottom edge exactly like every other list; the last item stays clear via the list’s existing `contentPadding(bottom = Dimensions.bottomSpacer)` (in BottomFloating mode it already includes the system nav bar inset + mini-player height — same spacer as the `HomeSongs` / `HomeAlbum` / `ArtistScreen` lists); the full-screen background0 stays behind the content (identical to other pages); the per-screen `miniPlayer` overlay renders nothing (`MainActivity` passes `miniPlayer = {}` — the visible mini-player is the global `CustomBottomSheet`), so nothing needed repositioning
  - Verification: user build + device-verified (user, emulator `5554`, 2026-09-26) — the list now scrolls under the transparent nav bar, no background strip behind the buttons; pixel before/after on the nav-bar strip (100% background0 with content stopped above it → list rows under the bar, matching the artist-page reference); review skipped on explicit user request
- feat(miniplayer): the global mini-player drops to the screen edge on pages without the app nav bar — route-aware bar reservation instead of the blind barless toggle (issue https://github.com/N-Zik-Group/N-Zik/issues/832 — 2026-09-26)
  - Root cause: `MainActivity` computed `hasNavBar = !areBarsHidden` (the landscape-barless toggle only) — unlike the `Skeleton` per-screen rule (`!hideNavBar && buttonList.size >= 2`), it was unaware of the current route, so on nav-bar-less routes (album, insights, playlists, podcasts, history, …) the mini-player kept reserving the bar height (floating: `barHeight + 4.dp`, standard-bottom: `standardNavBarHeight + 4.dp`) and floated with an empty strip below it
  - Fix: new `app.n_zik.android.utils.appNavBarPresentForRoute(route)` — whitelist of the 6 routes that render the app nav bar (`home`, `artist/{id}`, `settings?…`, `search?text=…`, `searchResults/…`, `statistics`), default `false` so every bar-less page — current and future — sits at the screen edge automatically; `MainActivity`: `hasNavBar = !areBarsHidden && appNavBarPresentForRoute(playerRoute)`; the existing 250 ms `animateDpAsState` tween animates the drop on route change; `playerPos == Top` is a separate branch (`5.dp`) — untouched
  - Review hardening: boundary guards keep lookalike routes out (`artist/` excludes `artistInsights`/`artistAlbums`, `search?` excludes `searchResults`, bare `startsWith("searchResults")` still matches the literal `Uri.encode`d routes); the route is sourced from `navController.currentBackStackEntryAsState()` instead of the Discord-RPC destination state so the layout never depends on that feature’s listener; dead `route == "search"` branch removed (the registered pattern is `search?text={text}`); known exception KDoc-documented — Home can fall back to a single tab (bar then not drawn) → phantom gap only, never an overlap
  - Tests: `AppNavBarRouteTest` (JUnit 5 — 3 tests: the 6 bar routes, the 19 bar-less routes, `null`, bare prefixes (`artist`/`settings`/`search`/`rewind`) and declared-but-unregistered routes (`games`/`videoOrSongInfo`), lookalike disambiguation); full module suite green except the pre-existing flaky `RescueScreenProcessStatusTest` (green in isolation)
  - Verified on device (user, 2026-09-26: « ça marche ! ») — the mini-player sits flush above the system nav bar on the album page, position unchanged on Home/Artist/Search/Settings/Statistics, animated glide on route transitions

Other:
- chore(db): detailed Timber logs across the whole artist-mapping cycle — play-time reconcile drops + startup sweep, zero behavior change (`51023e022f` + spec `_bmad-output/implementation-artifacts/spec-streaming-reconcile-diag-logs.md`)
  - `Database.kt`: the `upsert` add-only branch (the only path that leaves stale links) logs `upsert ADD-ONLY song=… names=… authors=… added=…` with a WHY comment; new `internal fun logReconcileDrops(mapTable, artistTable, songId, songTitle)` logs one `reconcile dropped link: song=… "…" artist="…"` line per about-to-be-dropped pair right before each `deleteBySongId` of the three reconcile sites (`upsert` RECONCILE, `insertIgnore` ids, `insertIgnore` names) — orphan artist rows log as `artist="null"`; all under the `Database` tag at `d` level (stripped in release), the existing summary lines untouched
  - `SongArtistMapTable.kt`: +`pairsBySongIdDirect(songId)` targeted `@Query` — the helper reads only that song's pairs (no full-table `allPairsDirect()` scan, no schema change)
  - `DbCleanup.kt`: the startup sweep now logs every removed link (`stale link removed: song=… "…" artist="…" artistsText="…"`, `DbCleanup` tag, `d`) before `deletePairDirect`; the `i` summary line is unchanged
  - Tests: `DbCleanupTest` +2 (Timber capture tree — per-pair line + `removed 1` summary on a stale DB; no per-pair line + `removed 0` on a clean sweep) and `DatabaseInsertIgnoreReconcileTest` +3 (ids → N lines before the summary + targeted-query verify; names-only incl. orphan `artist="null"`; no stale links → no per-pair line, summary `dropped=0`)
  - Verified on device (user, 2026-09-26): full playback capture — ~55 reconciles each logging exactly N lines for `dropped=N` (both `insertIgnore RECONCILE` and `upsert RECONCILE` branches confirmed), 10 songs with `dropped=0` log summary-only, and the startup sweep logged its 12 removed links pair by pair; the capture exposed two data-level findings — duplicate same-name `Artist` rows consolidated by the reconcile, and JP↔romaji name-variant links that the startup sweep removes right after a play-time reconcile re-maps them (pre-existing committed behavior, follow-up spec planned)
  - `assembleDebug` BUILD SUCCESSFUL; module suite green (only the pre-existing flaky `RescueScreenProcessStatusTest` fails in full runs, green in isolation)

Added:
- feat(song): unified song « Update » action — one checkbox dialog (title / artists / thumbnail / playtime / cache) replaces the player all-or-nothing refetch and the song-menu « Reset » (user-driven, no issue — 2026-09-26, spec `_bmad-output/implementation-artifacts/spec-song-update-unification.md`)
  - New `UpdateSongDialog` (`components/dialog/song/UpdateSongDialog.kt`): 5 checkboxes; a YTM fetch runs only when a fetched-field box is ticked (`needsFetch`), every stored value guarded by `retainIfModified` (a `modified:` value is never clobbered, a null fetched field keeps the stored value, a null fetch writes no fetched field at all); `ResetCache.kt` + `ResetSongDialog.kt` deleted (whole files)
  - Ticking « artists » re-links the artist links immediately: `ArtistMappingReconcile.parseAuthorEntries` (extracted + shared with `Database.upsert` so the entry rules never diverge — trim + NBSP-normalize, pure-separator and standalone-conjunction entries skipped) + `Database.reconcileArtistLinks` (drop the song's stale links, upsert the fresh artists, re-map, return the dropped count — behavior unchanged on the upsert path); the gate `shouldReconcileAuthors` (complete list per `isCompleteAuthorList`, stored artistsText not `modified:`)
  - Menus rewired: player-menu all-or-nothing refetch → the same dialog (position kept), song-menu « Reset » → « Update »; local-playlist toolbar action replaced too (user-approved legacy edit, `LocalPlaylistSongs.kt`: import, `reset_cache` → `update` toolbar branch, batch dialog call)
  - Bug found by the review test hardening: `Song.copy()` carries only constructor params, so every update silently reset the body property `playCount` to 0 — `applySongUpdate` now restores the stored value (the strengthened test pins it)
  - Review fix: confirming with no box ticked is now a true no-op (no DB write, no done toast — spec I/O row « Aucun fetch, aucun écrit en base »)
  - Strings: 4 new EN keys (`update_title_from_ytm`, `update_authors_from_ytm`, `update_thumbnail_from_ytm`, `info_open_update_dialog`) + the 6 reset keys deleted, `values/strings.xml` only (`values-*/` are Crowdin-managed — no hand edits)
  - Tests: `UpdateSongDialogTest` (18 — applySongUpdate / shouldReconcileAuthors incl. the playCount contract), `RunSongUpdateTest` (13 — fetch gate, checkbox wiring, channel-anchored resolution, reconcile wiring, junk-entry agreement between the two entry-rule copies), `ReconcileArtistLinksTest` (3 Robolectric/Room — placed in `core.database` per the mirroring rule, the `database/` package is grandfathered for pre-existing files only); `assembleDebug` + full suite green
- feat(songs): batch « Update » toolbar button on Home Songs — applies the unified Update dialog to the selected songs (or every song of the tab when nothing is selected) (user-driven, no issue — 2026-09-26, spec `_bmad-output/implementation-artifacts/spec-song-update-toolbar.md`)
  - `runSongUpdateBatch` (`UpdateSongDialog.kt`): sequential per-song `runSongUpdateCore` on `NzikDispatchers.DATA`; local songs skipped silently (no YTM id); a single summary toast replaces the per-song done toasts; cancellation is cooperative (between songs the batch returns the processed count with no toast, a cancel landing mid-song propagates `CancellationException`)
  - Rate limit (renegotiation, user-approved « infinis, tant que c'est rate limited »): random 1–5 s gap between YTM fetches (`fetchGapMs: () -> Long = { (1000L..5000L).random() }`, injectable to 0 in tests — the `HomeSyncService` pattern), no leading delay, applied only when a fetch box is ticked — NO per-launch cap
  - Button in the Home Songs toolbar (`HomeSongsScreen.kt`): the selected songs, or every song of the tab when nothing is selected; hidden on the OnDevice tab; the button id `update` registered in `HomeSongsToolbarSettingsDialog` (`allButtonIds` + per-tab availability, on by default, the user's persisted toggle respected) + `LocalPlaylistToolbarSettingsDialog` aligned (`reset_cache` → `update`)
  - Review fixes: a non-cancellation exception in the batch no longer leaves the progress dialog open (caught + logged + closed), a confirm with nothing ticked no longer rewrites N identical rows and toasts « Updated N songs », the batch KDoc re-attached from `remoteTargets` to `runSongUpdateBatch`
  - Strings: `updated_songs_count` (EN, `values/strings.xml` only)
  - Tests: `RunSongUpdateBatchTest` (9 — progress cadence, rate-limit gaps, cancel between songs / mid-song, empty targets) with `fetchGapMs = { 0L }`; `assembleDebug` + full suite green

Fixed:
- fix(migration): global same-name artist dedup at launch — the YTM search browse id as arbiter, persistent skip store, self-healing on every launch (user-driven, no issue — 2026-09-26, spec `_bmad-output/implementation-artifacts/spec-same-name-artist-dedup.md`)
  - Root cause: YTM attributes the same artist to several different browse ids per playback context (topic pages, dead channels, empty shells), so one real artist accumulates several same-name `Artist` rows each collecting its own song links (device dump: 208 groups / 494 rows)
  - `SameNameArtistDedup.kt` (new, `core/migration`, `DbCleanup` pattern): groups the `Artist` rows by name (case-insensitive — `ArtistTable.sameNameGroups()` / `allByNameIgnoreCase()`), two-phase identify→resolve — search YTM, the returned browse id is the truth (followed rows included, a followed row can itself be a topic page); a stored row whose id equals it is kept, the others merge into it — re-link then delete, zero orphans by construction (`SongArtistMapTable.dropLinksAlreadyOn` guards the (songId, artistId) primary key, follow/dislike carry-over); `LOCAL_ARTIST_` rows excluded; **in case of doubt, never merge**
  - Truth row pinned to the group name with the `modified:` custom prefix — protected from the `StreamResolver` background fetch (`retainIfModified`) and from the `DbCleanup` judgment; an out-of-group truth row (same id, different real name) is now found and pinned too (review fix — `insertIgnore` alone was a silent no-op there)
  - Image safety rule D4 (approved revision): the truth row's picture is the reference when it carries one, else the strict majority of the group's stored pictures (the search thumbnail is explicitly demoted to non-reference); a tie, or a lone row without a picture in a conflicting group, flags the row (kept, unmerged); review fix: a null-image row in a conflicting group is now flagged even when the truth row carries a picture
  - Resolver (review decision, user-approved): STRICT case-insensitive exact name match only — the top-result fallback could merge a group onto a DIFFERENT artist (homonyms) with no image check able to catch it (`pickExactArtistMatch` extracted + tested); no match → null → the group is skipped and retried on the next launch
  - Persistent skip store (`PrefsSkipStore`, SharedPreferences/JSON): a judged group with remaining rows is remembered with its composition — unchanged composition skips the network on the next launch, a changed one is re-searched, a fully resolved group forgets its entry; no-match / failed groups are NOT remembered (retry on the next launch, frozen behavior); a corrupted blob degrades silently to empty memory (no throw, store stays usable)
  - Rate limit: random 1–5 s gap between searches (`delay((1000L..5000L).random())`), NO per-launch cap (renegotiation, user-approved); per-group logs (skipped custom-name / offline / deferred / merged / kept); review fixes: no double-count resolved+skipped when the merge fails, the stale « capped network pass » comment in `MainApplication` corrected
  - Wired in `MainApplication.onCreate` right after `DbCleanup` (DATA fire-and-forget); string `same_name_artists_dedup_toast` (EN, `values/strings.xml` only)
  - Tests: `SameNameArtistDedupTest` (26 — arbiter, image rule incl. every D4 variant, skip store, no-match / offline, carry-over, no orphans, strict resolver) + `SameNameArtistDedupPrefsSkipStoreTest` (5 Robolectric — the production SharedPreferences/JSON implementation: round-trip, forget on null, cross-instance, corrupted blob); `assembleDebug` + full suite green
  - Device-verified (user, 2026-09-26): converged DB — final launch summary `groups=11 resolved=0 merged=0 flagged=0 skipped=2 deferred=9 localKept=0` (9 real homonyms + 2 no-match groups, no false merges)
