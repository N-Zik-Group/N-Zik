fix(rpc): album DB fallback + explicit marker + image-text/preview fixes
  - Album regression ("always Unknown Album"): the presence builders read only `mediaMetadata.albumTitle`, which is null for YouTube streams — new `MediaItemUtils.albumTitleOrDb()` (metadata → DB: extras `albumId` → `albumTable.findByIdDirect`, else `findBySongIdDirect`) applied at both presence sites in `DiscordPresenceManager`, same resolution order as the player screen; `resolveAlbumTitle` (pure) + `albumTitleWithFallback` (live Flow) feed the preview, and `artistTextWithFallback` gives the preview artist the same metadata → DB path as the manager's `artistTextOrDb`
  - Explicit marker on the RPC title, exactly like the notification: the app's own `MediaItem.isExplicit` (`Utils.kt`: `e:` title prefix + `is_explicit` extras + media3 standard key) at the 3 display sites (manager playing + paused builders, preview card); `cleanPrefix` runs first so the marker is never doubled; same `"\uD83C\uDD74 "` literal as the notification
  - Image text (artwork hover) built-in default is now the album value: new const `DiscordActivityBuilder.DEFAULT_LARGE_IMAGE_TEXT_TEMPLATE = "{album.name}"` drives the settings entry display, the dialog placeholder, the idle preview line and the tooltip; the old computed "details - state" value (`defaultImageText` = "titre - artiste") deleted
  - Reset button in every template dialog (`DiscordTemplateFieldDialog`, all 10 fields): empties the field (empty = built-in default applies) and saves immediately — safe because every builder consumer falls back to its default on a blank value (`R.string.reset`)
  - Preview parity & details: artwork + logo tap tooltips show the RENDERED content while media is loaded (template as-is otherwise); `cleanPrefix` on every preview display field; the pinned "Paused" card freezes its progress bar while the real player keeps playing
  - Audit follow-ups (full re-audit clean, targeted tests green): unused import removed, KDoc marker corrected (🔞→🅴), `!= "null"` guard added in `albumTitleOrDb` DB branch
  - Tests: new `MediaItemExplicitTest` (2) + `MediaItemUtilsAlbumTest` (8), `DiscordImageTooltipTest` extended to 5 — `:ComposeN-Zik:testDebugUnitTest` full suite green (739 tests), `:ComposeN-Zik:assembleDebug` green; device-verified by the user (marker + album shown on the real RPC, preview dialogs correct)
fix(rpc): template field dialogs adopt the toolbar settings dialogs' button pattern (Reset / Cancel / OK)
  - `DiscordTemplateFieldDialog` (all 10 template fields: name, state, details, pause, button 1/2 labels+URLs, image text, logo text) button row rebuilt on the `ToggleListDialog` pattern — Reset (`textDisabled`) / Cancel (red @ 0.3) / OK (accent-filled pill, `onAccent`), same order, styles and spacing as the home toolbar settings dialogs; app strings (`R.string.reset/cancel/ok`) replace the raw `android.R` ones
  - Behavior aligned with the toolbar dialogs: Reset now clears the draft only (empty = the built-in default template applies again) WITHOUT saving — the dialog stays open so the user confirms with OK or discards with Cancel (previously it saved the default and closed immediately); OK saves the draft, closes and toasts `toast_preference_saved`, exactly like the toolbar `onConfirm`; Cancel discards the draft
  - The draft-state contract (reset/confirm/cancel) extracted to `DiscordTemplateFieldActions` (`app.n_zik.android.extensions.discord`) used by the dialog buttons; `DiscordImageTooltipDialog`'s OK button restyled to the same accent pill for visual consistency
  - Tests: new `DiscordTemplateFieldActionsTest` (4) pinning the contract — reset clears without saving (no toast), confirm persists the draft + toasts preference saved + dismisses, confirm after reset persists the empty default, cancel dismisses without saving/toasting; Toaster stubbed `just Runs` (mockkObject is a spy — an unstubbed call runs the real body, whose `Toaster$Type` static init needs `android.graphics.Color` and fails on the JVM); 57/57 discord suite green
feat(menu): hamburger menu — Rescue Center + Debug entries
  - Final menu order: History, Statistics, PiP (conditional), divider, Settings, Debug, Rescue — no divider under Settings
  - Debug entry is a custom `Row` replicating the 1.5.0-alpha28 `DropdownMenuItem` metrics 1:1 (48dp min height, 12dp horizontal / 0dp vertical content padding, 24dp leading icon, 12dp icon-to-text gap, `labelLarge` text style — verified against the library sources and the user's Layout Inspector measurements); a single `combinedClickable` drives both gestures (short tap toggles debug logs with the same side effects as the Misc settings switch — "restart required" toast on enable, log files purged on disable via new `purgeDebugLogs`; long press deep-links to Misc settings landing on the Debug card). The stock `DropdownMenuItem` was dropped: it has no `onLongClick` parameter and its inner `clickable` blocks an outer long-press detection
  - Rescue entry: standard item, launches the lightweight `:rescue`-process `RescueActivity` with the same explicit intent as the launcher shortcut
  - Deep-link plumbing: settings nav route gained `?tab={tab}&focus={focus}` (defaults 0/""), `tab=7` = Miscellaneous, `focus=debug` triggers an animated scroll to the Debug card in `OtherSettings` (`positionInRoot` measurement on the scroll container + card wrapper, `animateScrollTo` — robust to the AnimatedContent tab transition); `HamburgerMenu` gained `navController` + `onItemConsumed` so item taps close the menu (also fixed the latent bug where Rescue left it open)
  - New code under `app.n_zik.android.*` only (`DebugLogsMenuItem` composable, `purgeDebugLogs`/`debugLogFiles` helpers); legacy files edited surgically, no new files in legacy packages; bugs icon scaled 0.9 (pivot 12,12) to match the app's menu-icon convention (stats/settings/history glyphs render at ~80–90% of the canvas)
  - Tests: new `DebugLogsTest` (3) green; layout device-verified by the user (menu order, tap toggle + long-press deep link, icon sizing)
feat(rewind): 16-image deck capture → SAF file-manager export, animations cut during the pass
  - Share button on the deck finale now opens a SAF folder picker (`OpenDocumentTree`, same flow as the cached-song `ExportCacheDialog`); the ACTION_SEND multi-image intent is gone — the system share sheet cannot carry a full deck through third-party apps. Picked folder is persisted with `takePersistableUriPermission` (read+write)
  - Capture: `PixelCopy` straight from the display framebuffer per deck page (API 34+, app `Window` + `OnPixelCopyFinishedListener`, main-looper handler); the deck plays itself and each settled page is copied + saved as `NZik_Rewind_<year>_01..16.png` (zero-padded so galleries order the 16 slides lexicographically). The display framebuffer is the only path that keeps the GPU-rendered HypnoticCanvas RuntimeShader backgrounds — a software canvas replay cannot draw a RuntimeShader
  - System bars hidden for the whole pass (`WindowCompat.getInsetsController(window, view)` + `hide(systemBars())`, `androidx.core.view.WindowInsets`) while the slide shells pin the ignoring-visibility bar insets through the new `rewindShareCaptureActive` snapshot state, so each exported image keeps exactly the live deck's top/bottom padding (insets never collapse to zero, no clock/battery/nav glyphs in the bar areas); bars + state restored in `try/finally` even on failure or cancellation
  - One-shot animations are cut while the capture runs so no page is ever screenshotted mid-reveal (the old per-page wait table was too short for the reveal chains and missed about half of the reveals): new `rewindRevealDelay` (stagger skipped) and `rewindDriveTo` (`Animatable` snapped instead of tweened) helpers wired into the shared reveal components (reveal / animated number / typewriter), the intro card gate (2 950 ms hold skipped), the top-song phase machine (jump straight to the revealed winner) and the five slide-level tweens; ambient loops (shader rotation, 7 s artwork crossfade) are left running
  - Per-page wait table (~34 s total) replaced by a uniform 900 ms settle → full 16-page pass ~18–20 s; the settle contract test pins the range (250..3 000 ms) and the pass budget (< 30 s)
  - Export: captured files are written into the picked folder through `DocumentsContract.createDocument` (one document per page, on `Dispatchers.IO`, `CancellationException` re-thrown), temp cache copies deleted on success; feedback via Kreate `Toaster.done()` / `Toaster.e(rw_error_export)`
  - Fallback below API 34 (no display-framebuffer PixelCopy overloads at runtime): software-replayed final slide only, exported to the same folder via the refactored `captureRewindScreenshot` (capture-only, returns the PNG file instead of firing a share intent)
  - Finale footer in shareMode: the listener-level line was a bare 9 sp cream @0.52 text — unreadable on the exported image; it is now a pill (cream surface, ink badge title, index in a lime circle, same visual language as the live listener-level row)
  - Compose 1.11.4 gotcha pinned: `Animatable` is `Animatable<T, V : AnimationVector>` (two type parameters) and `InterpolationAware` is not exposed in `animation.core` — the helper is Float-concrete with a star projection on the vector parameter, all call sites pass `Animatable(0f)`
  - Strings: dead `rw_share_*` / `rw_error_share_image` removed, `rw_error_export` added (base `values/strings.xml` only); ktfmt parse clean on every touched file; no legacy-package changes
  - Tests: new `RewindShareCaptureSettleTest` (2) replacing the wait-table contract, `RewindShareCapturePaddingHoldTest` (2) still green
  - Pending: user device verification (16 exported PNGs with bars hidden, padding kept, level pill readable, pass ~20 s). Next steps: rewind home page ("My Rewind" — year rows + 12-month grid with minutes/plays, empty months greyed) and monthly mode ("Wrapped du mois")
feat(rewind): "My Rewind" home page + monthly deck ("Wrapped du mois")
  - New `RewindHomeScreen` (route `rewindHome`, hamburger menu entry "Rewind" now navigates there): brand hero (title + tagline), one row per year (listening time + plays, newest first — tap selects the year and opens the annual deck), "MONTHS OF <year>" with year chips (switch the grid without opening the deck) and a 3x4 grid of the selected year's 12 months (localized month name, listening time, plays; months without events greyed @0.35 and not tappable, tap opens the monthly deck). Empty state (MusicNote + text) and themed `Loader()`; the selected year is `rememberSaveable` so it survives the round-trip into the deck
  - Home data in one pass: `RewindDataFetcher.getRewindHomeData()` groups all events (same source as the deck: `allWithSong`) by year → `RewindHomeYear(year, minutes, plays, months)` reusing `getMonthlyStats` for the 12-month breakdown, so the home page and the deck cannot disagree
  - Monthly deck: `getRewindData(year, month)` filters the full pipeline to the month boundaries; new pure helpers `rewindPeriodBoundaries` / `rewindDaysInPeriod` / `rewindPeriodLabel` shared by annual + monthly paths; `RewindData` now carries `periodLabel` ("2026" / "JANV. 2026" — localized short month name uppercased, dot normalized) + `daysInPeriod`; every slide display that showed the bare year now shows the period (all kickers, intro big label + tagline, finale kicker/heading/brand, top-song + album wall footers, loader words "YOUR MONTH IN MUSIC", listening-days ratio/days-without/percent) and the listening-days slide + average daily minutes use the period's day count
  - Route: `rewind?year={year}&month={month}` (Int args, defaults 0/-1 = current year / annual deck); the deck-only side effects — app header slide-out (`AppNavigation.isRewind`) and mini-player sheet dismissal (`MainActivity.isRewindDeck`) — now exact-match the deck route instead of `startsWith("rewind")`, so the home page keeps the header and the mini-player
  - Export file names: monthly decks add the zero-padded month (`NZik_Rewind_<year>_<MM>_NN.png`) on both the PixelCopy pass and the API <34 software fallback
  - Strings: home block (`rw_home_*`), kickers + finale heading/brand + "top five" / "#1 artist" kickers now formatted with `%1$s` (period label), `rw_top_album_kicker` → "TOP ALBUM", "OF THE YEAR" → "OF THE PERIOD" on the listening-days percent line, `rw_intro_tagline_month` + `rw_loader_word_month` for the monthly deck (base `values/strings.xml` only)
  - Tests: new `RewindPeriodBoundariesTest` (4) — year span, inclusive + contiguous month boundaries, December year rollover, leap-year day counts; ktfmt parse clean on every touched file; legacy edits surgical (NavRoutes `rewindHome`, AppNavigation route/args, ActionBar menu target, isRewind exact matches)
  - Pending: user device verification. Design question: the "month by month" slide inside a monthly deck shows a single bar — keep it, drop it for monthly decks, or turn it into a day-by-day view
feat(rewind): home page final — artwork-filled year cards, month collages, DAYS slide, oldest-first
  - Year rows now mirror the month cells 1:1: the year's top song / album / artist / playlist (`RewindHomeYear.topArtworks`) as a full-bleed 4-quadrant strip (equal `weight(1f)`), bounded 96 dp height, `clip(uiRoundnessShape())`, child order images → scrim → text, scrim 0.7, white centered text, no slashes; empty year = scrim 0.55 + launcher box, same treatment as an empty month. Root cause of the previously invisible scrim: a contentless `fillMaxSize` Box sizes to 0 when its height is unbounded (a LazyColumn item), so it had nothing to draw — the bounded height fixed "images par-dessus le calque noir"
  - Month cells now fill with a 2x2 collage of that month's top artwork (`RewindHomeMonthCollage`: BoxWithConstraints-driven cover mosaic sized to the cell height, `RoundedCornerShape(0.dp)` on the mosaic because the cell is already rounded by `uiRoundnessShape()`; empty month = launcher box @ 0.55)
  - New DAYS slide for the monthly deck (`RewindDaysCard`): one cell per calendar day from `calendarDayStats(year, month, events)` (per-day minutes + plays, built in `RewindDataFetcher`, carried on `RewindData.calendarDayStats` — empty for annual decks), peak-day highlight with peak day / peak minutes stats, inactive days dimmed
  - Year sort default flipped to `SortOrder.Ascending` (oldest first, arrow up); the year sort only reorders the year rows — the year chips keep the stable data order (newest first, `years.forEach`) and tapping a year card only opens that year's deck (`onOpenDeck(year, null)`), no longer switching the month grid's selected year (the chips remain the only way to switch years there)
  - Verified: every rounded surface on the home page uses the global `uiRoundnessShape()` (year rows, month cells, chips); the only hardcoded shapes are the two intentional `RoundedCornerShape(0.dp)` cover mosaics that fill already-clipped cells
fix(rewind): deck back returns to the rewind home + readable listener badge
  - `RewindScreen` gained `BackHandler { navController.popBackStack() }`: the deck is a full-screen HorizontalPager with no visible exit, and the app-wide "back goes home" preference (legacy `disableNavigationBackStack` callback in AppNavigation) pops past the rewind home all the way to the app home. The deck's handler registers after that callback, so any back event while the deck is on screen pops exactly one level, landing on the rewind home. A visible back button was added then removed at the user's request (they use the Android system back) — only the handler stays, imports cleaned
  - `RewindListenerBadgeCard` small titles enlarged: tier names (CURIOUS … BEYOND REPEAT) 6/8 sp → 9/11 sp (current tier), badge index label + sublabel 9 sp → 11 sp
